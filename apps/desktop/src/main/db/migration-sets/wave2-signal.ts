import type { Migration } from "../migrations";

/**
 * Wave 2 — WT-A (Signal & Attention) migrations. Owned by feat/wta-signal.
 * Id prefix `w2a_` — never edit another group's set file.
 */
export const wave2SignalMigrations: Migration[] = [];
