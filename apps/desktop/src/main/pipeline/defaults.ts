// Re-export from shared for backward compatibility

export type { PipelinePreset as DefaultPipelineTemplate } from "@exegol/shared";
export { PIPELINE_PRESETS as DEFAULT_PIPELINE_TEMPLATES } from "@exegol/shared";
