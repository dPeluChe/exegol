/**
 * MCP Server Registry — re-exports types from @exegol/shared.
 * Inspired by Zed's ContextServerRegistry and Nanobot's connect_mcp_servers.
 */

export type {
  McpServerConfig,
  McpServerState,
  McpServerStatus,
  McpTool,
  McpTransport,
} from "@exegol/shared";
