# Tasks Completed — May 2026

## 2026-05-21: Wave 1 — Stack Optimizations (Terax review quick wins)

### T108 — Vite Build & Bundle Optimizations (DONE)
Commit: `53bf397 build(T108): manualChunks + esbuild drop/pure + chrome134 target`

- [x] `electron.vite.config.ts`: renderer `build.rollupOptions.output.manualChunks` splits xterm, monaco, react-vendor, radix, trpc into separate chunks
- [x] `build.esbuild: { drop: ['debugger'], pure: ['console.debug', 'console.info', 'console.trace'] }`
- [x] `build.target: 'chrome134'` (Electron 41 supports natively)

### T109 — Tailwind v4 `@theme` Migration (ALREADY DONE)
- [x] Verified `globals.css` already uses `@import "tailwindcss"` + `@theme { ... }` block
- [x] No `tailwind.config.ts` present
- [x] No migration needed; removed from active backlog

### T110a — streamdown adoption (DONE)
Commit: `05fa51e feat(T110a): swap react-markdown for streamdown`

- [x] Swapped `Markdown` from `react-markdown` for `Streamdown` in `CodeViewer.tsx:MarkdownViewer`
- [x] Removed `react-markdown` from `apps/desktop/package.json`

### T110b — use-stick-to-bottom (DEFERRED)
- [x] Skipped: no streaming markdown/chat surface in current UI; xterm handles terminal scrollback internally
- [x] To revisit when chat composer or activity stream lands

### T110c — Diff cache LRU + inflight dedup (DONE)
Commit: `02b3730 feat(T110c): diff cache LRU + inflight Promise dedup`

- [x] New `apps/desktop/src/main/lib/lru-cache.ts` (`AsyncLruCache<K, V>` with `getOrCompute` + `invalidateWhere`)
- [x] Wired into `diff.ts`: `structuredDiff`, `projectDiff`, `stagedDiff` cache by `projectId|kind|staged|pathOverride`
- [x] Stage/unstage/commit mutations call `invalidateProjectDiff(projectId)`

### T111 — tokenlens model context registry (DONE)
Commit: `b72892c feat(T111): tokenlens for model context window registry`

- [x] Added `tokenlens` dep
- [x] `getModelMaxContext(model)` helper in `TokensSection.tsx` using `getContext()` from tokenlens
- [x] Model breakdown table shows `N ctx` badge per model row

### T121 — electron-window-state (DONE)
Commit: `b60e3db feat(T121): restore window size/position via electron-window-state`

- [x] Added `electron-window-state` dep
- [x] Wired into `createWindow()` in `main/index.ts`: `windowStateKeeper({ defaultWidth: 1400, defaultHeight: 900 })`, `state.manage(mainWindow)`

### T103 — Release Config Completion (DONE)

- [x] `electron-builder.ts`: replaced `owner: "OWNER"` placeholder with `dPeluChe`, removed TODO comments
- [x] New `scripts/release-checklist.md`: prereqs (Apple notarize creds, `GH_TOKEN`), per-release flow (preflight → build → publish → verify auto-update → post-release), stable vs canary channel separation table, common-failure troubleshooting
- [x] Canary channel auto-enabled via existing `generateUpdatesFilesForAllChannels: true`
- [x] Notarization stays `false` until Apple credentials configured; checklist flags it as prereq

---

## 2026-05: Preflight spawn check + security hardening

### T104 — Preflight Spawn Check (DONE)
Commit: `898de41 feat: T104 preflight spawn check + symlink-escape tests + FEATURES fix`

- [x] `runPreflight()` in `apps/desktop/src/main/agents/preflight.ts` — structured `PreflightResult { ok, warnings[], errors[] }`
- [x] Validates: CLI binary on PATH, API key present (when provider requires), git repo valid, access mode supported
- [x] Wired into `AgentManager.spawn()` before PTY creation
- [x] IPC procedure exposed via `apps/desktop/src/main/ipc/procedures/agents.ts`
- [x] Symlink-escape tests added to `path-guard.test.ts`
- [x] Updated `docs/project_definition/FEATURES.md`

## 2026-05: WT5 Codebase Hygiene Splits (DONE)

Six monolith files (>500 LOC) split into focused sub-modules — pure motion, no behavior change. All 273 JS tests + 22 Rust tests pass without modification. Public API preserved across every consumer.

| File | Before | After | Sub-files |
|---|---|---|---|
| `renderer/components/workspace/sections/TasksSection.tsx` | 505 | 330 | `tasks/use-persisted-task-file.ts` (23), `tasks/TasksEmptyState.tsx` (38), `tasks/TasksToolbar.tsx` (134), `tasks/task-file-actions.ts` (94) |
| `renderer/components/workspace/WorkspaceTabBar.tsx` | 544 | 303 | `WorkspaceTabItem.tsx` (121), `QuickLaunchBar.tsx` (154), `tab-bar-helpers.ts` (53) |
| `packages/core-rust/src/processing/status_parser.rs` | 557 | 145 | `status_matchers.rs` (264), `status_parser_tests.rs` (146); `processing/mod.rs` declares new submodules; `#[napi]` exports stay in `status_parser.rs` so `index.d.ts` is unchanged |
| `renderer/components/settings/TerminalSettings.tsx` | 587 | 182 | `terminal-settings/recommended-fonts.ts` (145), `FamilyBadge.tsx` (75), `FontCard.tsx` (120), `FontGroup.tsx` (76) |
| `main/ipc/procedures/diff.ts` | 714 | 220 | `diff-helpers.ts` (62), `diff-review.ts` (181), `diff-state.ts` (117), `diff-pr.ts` (75), `diff-ai.ts` (82); router still exported from `diff.ts`, PR + AI procedures composed via object spread |
| `renderer/components/workspace/BrowserPaneContent.tsx` | 799 | 323 | `BrowserAddressBar.tsx` (175), `BrowserQaRecordingBar.tsx` (155), `BrowserReplayResultBar.tsx` (72), `use-browser-qa.ts` (291) |

Commits:
- `3064890 refactor: split TasksSection.tsx into focused sub-modules`
- `b891119 refactor: split WorkspaceTabBar.tsx into TabItem/QuickLaunch/helpers`
- `b82bc66 refactor: split status_parser.rs into matcher + tests submodules`
- `b82c9d4 refactor: split TerminalSettings.tsx into FontCard/FontGroup/FamilyBadge`
- `d556b40 refactor: split diff.ts router into helpers/review/state/pr/ai`
- `344e490 refactor: split BrowserPaneContent.tsx into focused components and hook`

---

## 2026-05: WT3 — Rust Search Backend

### T116 — Rust search crates + napi exports + tRPC bridge (DONE)
Commits:
- `feat(T116): add ignore + grep-* + globset crates with napi fsSearch + fsGrep`
- `feat(T116): expose fsSearch + fsGrep via tRPC fs-search router`

- [x] Added Rust crates to `packages/core-rust/Cargo.toml`: `ignore = "0.4"`, `grep-regex = "0.1"`, `grep-searcher = "0.1"`, `grep-matcher = "0.1"`, `globset = "0.4"`, `tempfile = "3"` (dev)
- [x] New `packages/core-rust/src/search/` module: `mod.rs`, `types.rs` (SearchResult, SearchLimits, GrepHit, GrepOptions), `fuzzy.rs` (fixed-size min-heap scorer, gitignore-aware), `grep.rs` (parallel walker + grep-searcher with binary detection and 240-char line cap)
- [x] napi exports: `fsSearch(query, root, limits) -> SearchResult[]` and `fsGrep(pattern, root, opts) -> GrepHit[]` auto-surfaced in `packages/core-rust/index.d.ts`
- [x] New tRPC router `fsSearchRouter` in `apps/desktop/src/main/ipc/procedures/fs-search.ts` with `fuzzyFind` + `grep` procedures; registered in `apps/desktop/src/main/ipc/router.ts` as `fsSearch`
- [x] Rust unit tests: 21 new (9 fuzzy + 12 grep) using `tempfile::TempDir` — gitignore, hidden, max_results, max_depth, empty query, basename ranking, regex errors, globs, file-size cap, binary skip, line truncation, column offsets
- [x] TS bridge tests: 11 new (vitest, mocked `coreRust`) — input forwarding, Zod rejections (empty root, oversized caps, negative depth, empty pattern, empty glob), graceful degradation when native module is null
- [x] Quality gates green: `cargo check` / `cargo clippy -- -D warnings` / `cargo test` (42 passed) / `bun run typecheck` / biome / `vitest run` (284 passed) / `bun run build`
- [x] Bundle delta on `.node` artifact: 1.2 MB → 3.0 MB (+1.8 MB, release profile w/ LTO + strip)
- [x] Did NOT touch the existing FTS5 `searchRouter` in `procedures/search.ts` — contract preserved
- [x] No new TypeScript/npm dependencies
- [x] Foundation only: no UI consumes the endpoints yet (Command Palette wiring is downstream work)
