# Tasks Completed — May 2026

## 2026-05-21: WT4 — Parallel Multi-Agent + Agent UX

### Refactors (pure motion, zero behavior change)
- `c254100 refactor: split manager.ts into focused modules` — extract worktree setup + PTY invocation into `agent-spawn-flow.ts` (manager.ts 485 → 257 LOC).
- `69a9073 refactor: split TerminalPanel.tsx into Toolbar + Scrollback + helpers` — new files: `TerminalScrollback.tsx`, `TerminalToolbar.tsx`, `TerminalFloatingButtons.tsx`, `use-terminal-lifecycle.ts` (TerminalPanel.tsx 598 → 271 LOC; all sibling files ≤ 173 LOC).

### T65 — Parallel Multi-Agent on Worktrees (gap fill, P0)
`93d710e feat(T65): wire parallel run completion detection + broadcast`

The DB schema, spawn modal, and IPC stubs already existed — this lands the lifecycle gaps:
- `handleParallelAgentExit` (new `agent-parallel-orchestration.ts`) runs in `agent-session-callbacks.onExit`. When all sibling agents are terminal, the run transitions to `completed` (any completed agent) or `failed` (none completed). No-op while siblings still run; idempotent on a settled run.
- `promoteParallelAgent` (replaces the bare DB update) validates the agent belongs to the run, marks the run `completed`, sets `promoted_agent_id`, and broadcasts `parallel-run:changed`. Sibling worktrees retained (user inspects).
- 8 unit tests covering completion detection, mixed outcomes, idempotency, and promotion guards.

### T107 — Agent Comparator UI (P1)
`b349e85 feat(T107): comparator UI for parallel runs`

- `enrichParallelRunForComparison`: one server-side helper bundles diff stat (Rust `getDiff`), score, cost (token_usage SUM), duration, and scrollback tail per agent — single IPC round trip, N work on the main process.
- `agents.getParallelRunDetails` tRPC query.
- New Project sub-tab "Parallel Runs" with a list view → click → comparator with responsive auto-fit grid. Promote + Open buttons per column. Refetches every 10s while running, invalidates on promote.
- `ParallelSpawnModal` now navigates to the new sub-tab on spawn so the user lands directly in the comparator.

### T105 — Worktree Isolation Status Badge (P1)
`cbb89ac feat(T105): worktree isolation status badge`

- Migration 036 appends `isolation_mode` column to agents.
- `setupAgentCwd` writes the mode at spawn time: `pipeline` (cwdOverride), `isolated` (worktree created/reused), `fallback` (worktree requested but coreRust missing OR createManagedWorktree threw), `project-root` (useWorktree=false).
- `deriveIsolationMode` pure helper + 5 unit tests in `@exegol/shared`.
- `IsolationModeBadge` in `TerminalToolbar` — green/yellow/red color-coding with icon + tooltip showing the branch name.

### T106 — Agent Stop Reason Panel (P1)
`8ada7c2 feat(T106): agent stop-reason panel with resume/new-task/diff actions`

- `AgentStopReason.tsx` mounts above the read-only scrollback in `TerminalScrollback`. Shows status label, CLI type, exit code + exit reason (from agent_scores), scrollback tail (12 lines).
- Three actions: Resume session (only when `resumeCommand` is present on the agent record), New agent with same task (dispatches `exegol:spawn-agent` pre-filled), View diff (dispatches `exegol:view-diff`).
- `resume_command` now mapped through the Agent row + zod schema and exposed on the `Agent` type.

### Deferred follow-ups
- Component tests for `AgentStopReason` and the comparator: skipped because the project has no `@testing-library/react` setup and the brief forbids new dependencies. Pure helpers (`deriveIsolationMode`, parallel orchestration logic, completion detection) ARE unit-tested.
- The `exegol:view-diff` event has no listener yet in this branch — the action dispatches the intent; the receiver (a router that switches to the git pane scoped to the agent) is a separate cross-cutting task.
- Comparator only shows insertions/deletions/files (no test status column) — `agent_scores` has `tests_passed` but not every run populates it; can be added when scoring writes that field reliably.

---

## 2026-05-21: Wave 1 — Stack Optimizations (Terax review quick wins)

### T108 — Vite Build & Bundle Optimizations (DONE)
Commit: `53bf397 build(T108): manualChunks + esbuild drop/pure + chrome134 target`

- [x] `electron.vite.config.ts`: renderer `build.rollupOptions.output.manualChunks` splits xterm, monaco, react-vendor, radix, trpc into separate chunks
- [x] `build.esbuild: { drop: ['debugger'], pure: ['console.debug', 'console.info', 'console.trace'] }`
- [x] `build.target: 'chrome134'` (Electron 41 supports natively)

### T109 — Tailwind v4 `@theme` Migration (ALREADY DONE)
- [x] Verified `globals.css` already uses `@import "tailwindcss"` + `@theme { ... }` block
- [x] No `tailwind.config.ts` present
- [x] No migration needed; removed from active backlog

### T110a — streamdown adoption (DONE)
Commit: `05fa51e feat(T110a): swap react-markdown for streamdown`

- [x] Swapped `Markdown` from `react-markdown` for `Streamdown` in `CodeViewer.tsx:MarkdownViewer`
- [x] Removed `react-markdown` from `apps/desktop/package.json`

### T110b — use-stick-to-bottom (DEFERRED)
- [x] Skipped: no streaming markdown/chat surface in current UI; xterm handles terminal scrollback internally
- [x] To revisit when chat composer or activity stream lands

### T110c — Diff cache LRU + inflight dedup (DONE)
Commit: `02b3730 feat(T110c): diff cache LRU + inflight Promise dedup`

- [x] New `apps/desktop/src/main/lib/lru-cache.ts` (`AsyncLruCache<K, V>` with `getOrCompute` + `invalidateWhere`)
- [x] Wired into `diff.ts`: `structuredDiff`, `projectDiff`, `stagedDiff` cache by `projectId|kind|staged|pathOverride`
- [x] Stage/unstage/commit mutations call `invalidateProjectDiff(projectId)`

### T111 — tokenlens model context registry (DONE)
Commit: `b72892c feat(T111): tokenlens for model context window registry`

- [x] Added `tokenlens` dep
- [x] `getModelMaxContext(model)` helper in `TokensSection.tsx` using `getContext()` from tokenlens
- [x] Model breakdown table shows `N ctx` badge per model row

### T121 — electron-window-state (DONE)
Commit: `b60e3db feat(T121): restore window size/position via electron-window-state`

- [x] Added `electron-window-state` dep
- [x] Wired into `createWindow()` in `main/index.ts`: `windowStateKeeper({ defaultWidth: 1400, defaultHeight: 900 })`, `state.manage(mainWindow)`

### T103 — Release Config Completion (DONE)

- [x] `electron-builder.ts`: replaced `owner: "OWNER"` placeholder with `dPeluChe`, removed TODO comments
- [x] New `scripts/release-checklist.md`: prereqs (Apple notarize creds, `GH_TOKEN`), per-release flow (preflight → build → publish → verify auto-update → post-release), stable vs canary channel separation table, common-failure troubleshooting
- [x] Canary channel auto-enabled via existing `generateUpdatesFilesForAllChannels: true`
- [x] Notarization stays `false` until Apple credentials configured; checklist flags it as prereq

---

## 2026-05-21: WT1 — Terminal Foundations (Wave 1 PTY hardening)

Three tasks bundled in worktree `0521_wt1_foundations`. Reference impl lifted from Terax-AI
(`terax-ai/src-tauri/src/modules/pty/scripts/`, `terax-ai/src/modules/terminal/lib/{osc-handlers,dormantRing}.ts`).

### Refactor — Split `TerminalInstance.tsx` (425 LOC → 253 LOC)

- [x] `terminal-types.ts` — `TerminalInstanceHandle/Props`, themes, `CANVAS_ONLY_CLI_TYPES`
- [x] `terminal-buffer.ts` — `getScrollPosition` helper
- [x] `terminal-webgl.ts` — `createWebglController` (attach + context-loss recovery, T113 lives here)
- [x] `terminal-dormant-wiring.ts` — `createDormantPipe` (T115 wiring)
- [x] `terminal-setup.ts` — terminal/addon/PTY/OSC bring-up extracted from the giant mount effect
- [x] Public API unchanged: `TerminalPanel.tsx` continues to import `TerminalInstance` + handle type from the same path
- [x] Every new file ≤ 300 LOC (largest: `TerminalInstance.tsx` 253, `terminal-setup.ts` 163)

### T112 — OSC 7 + OSC 133 Shell Integration

- [x] Init scripts in `apps/desktop/src/main/terminal/shell-integration/` (`zshenv.zsh`, `zprofile.zsh`, `zlogin.zsh`, `zshrc.zsh`, `bashrc.bash`, `profile.ps1`) — lifted from Terax, names rebranded; existing OSC-777 ready marker re-emitted so `pty-shell-ready.ts` gating keeps working
- [x] `ensureShellIntegration()` materializes the scripts under `~/.exegol/shell-integration/` on startup (bundled via Vite `?raw` imports + `apps/desktop/src/main/env.d.ts`)
- [x] `AgentManager.spawn()` for plain shells now uses `getShellIntegrationZdotdir()` (zsh) / `getShellIntegrationBashRcfile()` (bash) — agent CLIs untouched
- [x] Renderer parser at `apps/desktop/src/renderer/components/terminal/osc-handlers.ts`: `registerOscHandlers` + `createShellIntegrationState`, with `parseOsc7` (URL-decoded, Windows drive-letter normalized) and OSC 133 A/B/C/D state machine + exit-code surfacing
- [x] **SSH spoofing guard**: OSC 7 inside `inCommand === true` is dropped — verified by `osc-handlers.test.ts`
- [x] Workspace store: additive `paneCwd`, `paneLastExit` slices + `setPaneCwd`, `setPaneLastExit`, `clearPaneShellState` actions (in-memory only, not persisted)
- [x] `TerminalInstance.tsx` derives `paneId` from `agentId` via a workspace-store lookup so we don't need to touch `TerminalPanel.tsx` (WT4-owned)

### T113 — PTY Flusher Hardening + WebGL Recovery

- [x] New pure module `apps/desktop/src/main/terminal/pty-sidecar-flusher.ts` with `appendPending` — 4 ms coalesce, 4 MiB `MAX_PENDING_BYTES`; on overflow drops pending and prepends `OVERFLOW_NOTICE = "\x1bc\x1b[2m[exegol: pty output buffer overflowed — earlier data dropped]\x1b[0m\r\n"`. Never slices a chunk mid-byte.
- [x] `pty-sidecar-entry.ts` switched from direct broadcast to per-session pending buffer + `scheduleFlush`. `flushSession` called on exit / destroy / shutdown.
- [x] `terminal-webgl.ts` `createWebglController` listens for `webglcontextlost` on the canvas the addon owns; on loss waits 250 ms and re-attaches; **MAX_RETRIES = 3**, then falls back to canvas renderer (the gap in Terax's impl). `webglcontextrestored` resets the counter.
- [x] Tests: `pty-sidecar-flusher.test.ts` covers under-cap concat, oversized single chunk, pending+new overflow, ESC c prefix, empty data. `terminal-webgl.test.ts` mocks `WebglAddon` and verifies 3 retries → fallback.

### T115 — DormantRing for Hidden Panes

- [x] `apps/desktop/src/renderer/lib/dormant-ring.ts` — 256 KB / 256 chunk bounded ring; on overflow keeps the tail + prepends `\x1bc[exegol: dropped output while hidden]\r\n`. Pure, no DOM.
- [x] `terminal-dormant-wiring.ts` `createDormantPipe` glue — single-branch hot path: writes go to xterm when visible, ring when hidden. Drain on un-hide replays in one shot.
- [x] `TerminalInstance.tsx` wires the pipe between the PTY data listener and `terminal.write`, driven by the existing `IntersectionObserver` visibility signal.
- [x] Tests in `dormant-ring.test.ts`: under-cap drain, single oversized write, ring overflow, chunk-count cap (257 → 256 retained), `clear()` reset, `isEmpty()/didOverflow()` after drain.

### Quality gates (all green)

- `bun run typecheck` ✓ · `npx @biomejs/biome check apps/ packages/shared/src/ packages/ui/src/` ✓
- `bun run test` 298 passed (19 new + existing) ✓
- `cargo check && cargo clippy -- -D warnings && cargo test` 22 passed ✓
- `bun run build` ✓ (shell-integration scripts inlined in main bundle via `?raw`)

---

## 2026-05: Preflight spawn check + security hardening

### T104 — Preflight Spawn Check (DONE)
Commit: `898de41 feat: T104 preflight spawn check + symlink-escape tests + FEATURES fix`

- [x] `runPreflight()` in `apps/desktop/src/main/agents/preflight.ts` — structured `PreflightResult { ok, warnings[], errors[] }`
- [x] Validates: CLI binary on PATH, API key present (when provider requires), git repo valid, access mode supported
- [x] Wired into `AgentManager.spawn()` before PTY creation
- [x] IPC procedure exposed via `apps/desktop/src/main/ipc/procedures/agents.ts`
- [x] Symlink-escape tests added to `path-guard.test.ts`
- [x] Updated `docs/project_definition/FEATURES.md`

## 2026-05: WT5 Codebase Hygiene Splits (DONE)

Six monolith files (>500 LOC) split into focused sub-modules — pure motion, no behavior change. All 273 JS tests + 22 Rust tests pass without modification. Public API preserved across every consumer.

| File | Before | After | Sub-files |
|---|---|---|---|
| `renderer/components/workspace/sections/TasksSection.tsx` | 505 | 330 | `tasks/use-persisted-task-file.ts` (23), `tasks/TasksEmptyState.tsx` (38), `tasks/TasksToolbar.tsx` (134), `tasks/task-file-actions.ts` (94) |
| `renderer/components/workspace/WorkspaceTabBar.tsx` | 544 | 303 | `WorkspaceTabItem.tsx` (121), `QuickLaunchBar.tsx` (154), `tab-bar-helpers.ts` (53) |
| `packages/core-rust/src/processing/status_parser.rs` | 557 | 145 | `status_matchers.rs` (264), `status_parser_tests.rs` (146); `processing/mod.rs` declares new submodules; `#[napi]` exports stay in `status_parser.rs` so `index.d.ts` is unchanged |
| `renderer/components/settings/TerminalSettings.tsx` | 587 | 182 | `terminal-settings/recommended-fonts.ts` (145), `FamilyBadge.tsx` (75), `FontCard.tsx` (120), `FontGroup.tsx` (76) |
| `main/ipc/procedures/diff.ts` | 714 | 220 | `diff-helpers.ts` (62), `diff-review.ts` (181), `diff-state.ts` (117), `diff-pr.ts` (75), `diff-ai.ts` (82); router still exported from `diff.ts`, PR + AI procedures composed via object spread |
| `renderer/components/workspace/BrowserPaneContent.tsx` | 799 | 323 | `BrowserAddressBar.tsx` (175), `BrowserQaRecordingBar.tsx` (155), `BrowserReplayResultBar.tsx` (72), `use-browser-qa.ts` (291) |

Commits:
- `3064890 refactor: split TasksSection.tsx into focused sub-modules`
- `b891119 refactor: split WorkspaceTabBar.tsx into TabItem/QuickLaunch/helpers`
- `b82bc66 refactor: split status_parser.rs into matcher + tests submodules`
- `b82c9d4 refactor: split TerminalSettings.tsx into FontCard/FontGroup/FamilyBadge`
- `d556b40 refactor: split diff.ts router into helpers/review/state/pr/ai`
- `344e490 refactor: split BrowserPaneContent.tsx into focused components and hook`

---

## 2026-05: WT3 — Rust Search Backend

### T116 — Rust search crates + napi exports + tRPC bridge (DONE)
Commits:
- `feat(T116): add ignore + grep-* + globset crates with napi fsSearch + fsGrep`
- `feat(T116): expose fsSearch + fsGrep via tRPC fs-search router`

- [x] Added Rust crates to `packages/core-rust/Cargo.toml`: `ignore = "0.4"`, `grep-regex = "0.1"`, `grep-searcher = "0.1"`, `grep-matcher = "0.1"`, `globset = "0.4"`, `tempfile = "3"` (dev)
- [x] New `packages/core-rust/src/search/` module: `mod.rs`, `types.rs` (SearchResult, SearchLimits, GrepHit, GrepOptions), `fuzzy.rs` (fixed-size min-heap scorer, gitignore-aware), `grep.rs` (parallel walker + grep-searcher with binary detection and 240-char line cap)
- [x] napi exports: `fsSearch(query, root, limits) -> SearchResult[]` and `fsGrep(pattern, root, opts) -> GrepHit[]` auto-surfaced in `packages/core-rust/index.d.ts`
- [x] New tRPC router `fsSearchRouter` in `apps/desktop/src/main/ipc/procedures/fs-search.ts` with `fuzzyFind` + `grep` procedures; registered in `apps/desktop/src/main/ipc/router.ts` as `fsSearch`
- [x] Rust unit tests: 21 new (9 fuzzy + 12 grep) using `tempfile::TempDir` — gitignore, hidden, max_results, max_depth, empty query, basename ranking, regex errors, globs, file-size cap, binary skip, line truncation, column offsets
- [x] TS bridge tests: 11 new (vitest, mocked `coreRust`) — input forwarding, Zod rejections (empty root, oversized caps, negative depth, empty pattern, empty glob), graceful degradation when native module is null
- [x] Quality gates green: `cargo check` / `cargo clippy -- -D warnings` / `cargo test` (42 passed) / `bun run typecheck` / biome / `vitest run` (284 passed) / `bun run build`
- [x] Bundle delta on `.node` artifact: 1.2 MB → 3.0 MB (+1.8 MB, release profile w/ LTO + strip)
- [x] Did NOT touch the existing FTS5 `searchRouter` in `procedures/search.ts` — contract preserved
- [x] No new TypeScript/npm dependencies
- [x] Foundation only: no UI consumes the endpoints yet (Command Palette wiring is downstream work)

---

## 2026-05-21: WT2 — Security Hardening (defense-in-depth)

### T117 — path-guard + command-guard (DONE)

- [x] `apps/desktop/src/main/security/path-guard.ts`: added `hasBidiChars` (Trojan Source U+202A–U+202E, U+2066–U+2069), `hasAdsSuffix` (NTFS Alternate Data Streams, drive-letter aware), `isSensitivePath` (`.env*`, `.ssh/`, `.aws/credentials`, `.config/gh`, `.gnupg`, keychains, `*.pem`, `*.key`, etc.), and `assertSafePath` — one-shot guard that throws typed `PathGuardError { reason, path }` and returns the realpath-resolved canonical path on success.
- [x] New `apps/desktop/src/main/security/command-guard.ts` with `inspectCommand(cmd) -> CommandRefusal` — refuses fork bombs, `rm -rf` on `/` / `~` / `$HOME` / `.` / `..` / `*` (case-insensitive flag combinations + long options + `--no-preserve-root`), `dd of=/dev/{sd,hd,nvme,disk}`, `curl|sh` / `wget|bash`, and bidi-override characters. Side-effect free; pre-compiled regex at module scope.
- [x] Test coverage extended: `path-guard.test.ts` adds parameterized cases for every new helper plus a symlink-to-`.ssh/id_rsa` Trojan case; `command-guard.test.ts` covers every refusal category plus benign `npm`/`bun`/`git`/`rm -rf node_modules`/`dd if=/tmp/x of=/tmp/y` cases.
- [x] **Deferred**: wiring `inspectCommand` into the agent spawn path requires touching `main/agents/manager.ts` which is WT4 territory. Helper + tests shipped; WT4 or a follow-up PR can call `inspectCommand(shellCommand)` before `spawn()` and surface refusals through preflight.

### T118 — Renderer CSP tightening (DONE)

- [x] `apps/desktop/src/renderer/index.html`: removed `https://cdn.jsdelivr.net` (unused by our code), dropped `'unsafe-eval'` (not present, kept absent), added explicit `connect-src 'self'` (all external HTTPS calls — Anthropic, GitHub — happen from the main process, not the renderer), added `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, `font-src 'self' data:`. Kept `frame-src *` / `child-src *` for the browser-pane `<webview>` with an inline comment explaining why future maintainers must not tighten them blindly.

### T119 — Preload capability allowlist (DONE)

- [x] New `apps/desktop/src/preload/capabilities.json`: declarative allowlist of every tRPC router (`projects`, `agents`, `settings`, `tokenUsage`, `resources`, `apiKeys`, `scheduler`, `files`, `prompts`, `diff`, `diffComments`, `scrollback`, `scoring`, `oplog`, `pipeline`, `skills`, `skillInstaller`, `mcp`, `memory`, `messages`, `queue`, `qaTests`, `activities`, `search`, `github`, `indexer` — all `"*"`) and every raw IPC channel currently used (`terminal:*`, `app:version`, `menu:*`, `agent:*`, `metrics:update`, `pipeline:status-changed`, `dialog:showOpenDialog`, `window:*`, `notification:navigate`, `updater:*`, `browser:*`, `floating:*`).
- [x] New `apps/desktop/src/main/ipc/capabilities.ts`: shared `isTrpcPathAllowed` + `isIpcChannelAllowed` + `CapabilityDeniedError` helpers that load the same JSON.
- [x] `apps/desktop/src/preload/index.ts`: every `ipcRenderer.invoke/send/on` call routed through an allowlist gate; refusals log `[capabilities] Capability denied: …` and throw a typed error so TanStack Query surfaces them naturally.
- [x] `apps/desktop/src/main/ipc/trpc-ipc.ts`: defense-in-depth re-check on every `trpc` invoke against the same JSON.
- [x] `docs/ARCHITECTURE/CAPABILITIES.md`: threat model (XSS in renderer bounded to declared capabilities), file format, two-layer enforcement, and how to add a new capability.
