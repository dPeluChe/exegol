# Tasks Completed — March 2026

## 2026-03-17: Project Inception & First Functional Iteration

### Research Phase
- [x] Analyzed Gemini research document on AI development platforms
- [x] Researched 11 tools/platforms in parallel (7 agents):
  - OpenAI Codex App (Electron + React + Rust, reverse-engineered stack)
  - Superset (7.2k stars, Electron + Mastra fork)
  - Cmux (Swift + libghostty, macOS only)
  - Conductor (2 products: Melty Labs desktop + Gemini CLI extension)
  - T3 Code (alpha, Codex-only)
  - Aider (41k stars, Tree-sitter repo maps, edit formats)
  - Mastra ($13M seed, 16k stars, TypeScript agent framework)
  - AgentFS (alpha, SQLite virtual filesystem)
  - Tree-sitter (165+ languages, Rust-native bindings)
  - MCP (10,850+ servers, spec 2025-11-25)
  - Tauri 2 (lighter than Electron but WebView inconsistency)
- [x] Researched CodexBar and token tracking ecosystem (17+ tools)
- [x] Researched libSQL vs SQLite (native vectors, encryption, multi-writer)

### Documentation Phase
- [x] Created docs/project_definition/README.md (vision, differentiators)
- [x] Created docs/project_definition/RESEARCH.md (11 tools + libSQL + CodexBar)
- [x] Created docs/project_definition/COMPETITORS.md (matrix + weakness map)
- [x] Created docs/project_definition/STACK.md (tech decisions + schema + rejected techs)
- [x] Created docs/project_definition/ARCHITECTURE.md (modules, data flow, monorepo structure)
- [x] Created docs/project_definition/FEATURES.md (4-phase roadmap, 14 Phase 1 features)
- [x] Created docs/project_definition/DESIGN_PATTERNS.md (10 patterns + 5 anti-patterns)
- [x] Updated docs with libSQL, scheduler, agent live status, token monitoring

### Implementation Phase — Initial Scaffold (commit c6c8d72)
- [x] Root monorepo: package.json (Bun 1.2.0), turbo.json, biome.json, tsconfig.base.json, .gitignore
- [x] @exegol/shared: 6 type files (agent, project, settings, scheduler, token-usage, worktree) + 3 Zod schema files
- [x] @exegol/ui: 6 Radix primitives (Button, Badge, Input, ScrollArea, Tooltip, Separator) + cn() utility
- [x] @exegol/core-rust: napi-rs scaffold + git2 worktree functions (Rust compiles clean)
- [x] Desktop main process: Electron entry, libSQL client + 9 initial migrations, DB queries, tRPC IPC bridge (createCaller proxy traversal), AgentManager, StatusParser, PtyManager, preload script
- [x] Desktop renderer: React 18 + TailwindCSS 4, initial components, Zustand stores, tRPC client hooks, xterm.js terminal
- [x] 92 files, 9,170 lines

### Code Quality Phase
- [x] Integration review: fixed 12 cross-package issues (deps, imports, tRPC paths)
- [x] Code quality review: identified 5 critical + 13 important + 12 nice-to-have issues
- [x] Fixed all 5 critical issues (process cleanup, path traversal, command injection, race conditions)
- [x] Fixed all 13 important issues (agent filtering, error boundary, preload APIs, buffer limits)
- [x] Fixed 8 nice-to-have issues (CLAUDE.md, Zustand stores, duplicate initTRPC, clippy)

### Runtime Debugging
- [x] Fixed circular dependency in tRPC router (extracted trpc.ts)
- [x] Fixed tRPC Proxy traversal (hasOwnProperty doesn't work on Proxies — use direct property access)
- [x] Fixed Zod validation (null vs undefined in Electron IPC serialization)
- [x] Fixed Zustand infinite re-render loop (getAgentList() in selector creates new array each render)
- [x] Fixed node-pty posix_spawnp: shell-based spawn ($SHELL -ilc) + @electron/rebuild for native module
- [x] Fixed xterm.js package rename (xterm -> @xterm/xterm v6)

### Milestone: First Successful Agent Spawn (commit c6c8d72)
- App launches, UI renders, projects can be added
- Claude Code spawned and executed inside Exegol terminal

### UI Improvements Pass (commit a2795ac)
- [x] Extracted reusable common components: EmptyState, KeyValue, LoadingSpinner, StatusDot, ConfirmDialog
- [x] Migrated from inline styles to TailwindCSS 4 utility classes throughout
- [x] Consistent color tokens via CSS variables (bg-primary, bg-secondary, text-primary, text-muted, etc.)
- [x] Code cleanup across renderer components

### Session Persistence & Settings Tabs (commit c4046db)
- [x] Added Zustand persist middleware to useAppStore (activeProjectId, activeView, sidebarCollapsed survive restart)
- [x] Settings panel redesigned with 4 vertical tabs: General, Agent CLIs, Terminal, Shortcuts
- [x] GeneralSettings: default IDE, theme, global hotkey
- [x] CliSettings: configure command + args + env per CLI type
- [x] TerminalSettings: font size, font family
- [x] KeyboardShortcuts: display all shortcuts
- [x] Settings persisted to DB via settings table (key-value store)
- [x] Workspace redesigned with 6 sections: Agents, Tasks, Diff Viewer, Scheduler, Token Usage, Resources
- [x] WorkspaceTabs component with icon + label + active underline indicator
- [x] Section components: AgentsSection (functional), plus placeholder stubs for Tasks, Diff, Scheduler, Tokens, Resources

### Sidebar Redesign (commit 93cc5eb)
- [x] Extracted SidebarSection — reusable collapsible section with chevron, icon, count badge, action slot
- [x] Projects section with count badge and "+" add button
- [x] ProjectsSection: shows project list in sidebar, click to select
- [x] Nested agents under selected project (via AgentCard)
- [x] Compact layout: reduced padding, smaller fonts (10-11px for section headers)
- [x] SidebarHeader and SidebarFooter extracted as separate components

### Collapsible Sections & Sidebar Footer (commit a60d0e1)
- [x] SidebarFooter with 3 sections: Schedulers overview, Resources overview, bottom bar
- [x] Schedulers overview (placeholder): collapsible, shows "No scheduled tasks"
- [x] Resources overview: collapsible, shows system CPU/RAM/disk from tRPC resources.getSystem
- [x] "All Projects" button in footer to navigate back to project list
- [x] App version display in footer (via app:version IPC handler)
- [x] RecentSessions section (collapsible, default closed)

### Hotkeys, Resources Monitoring, Shortcuts UI (commit d077e5c)
- [x] useHotkeys hook: Cmd+B sidebar, Cmd+, settings, Cmd+Shift+P projects, Cmd+N new agent, Cmd+. stop agent, Cmd+]/[ tab navigation, Cmd+1-9 agent switch
- [x] Events dispatched via CustomEvent for cross-component communication (exegol:spawn-agent, exegol:stop-agent)
- [x] Resources monitoring wired: ResourcesOverview in sidebar polls tRPC resources.getSystem
- [x] KeyboardShortcuts component: categorized display of all shortcuts

### Background Metrics Collector & Final Cleanup (commit f97f52e)
- [x] Background metrics collector in main process (startMetricsCollector/stopMetricsCollector)
- [x] Non-blocking: CPU delta-based (no 200ms sleep), RAM via vm_stat on macOS, disk via df
- [x] Collector runs every 10s, caches result for synchronous reads
- [x] Renderer polls at matching 10s interval (reduced from excessive polling)
- [x] KeyboardShortcuts redesigned: segmented tab control by category (Navigation, Agents, Terminal)
- [x] Migration 010: adds 'stopped' to agent status CHECK constraint (table recreation)
- [x] Stale agent cleanup on startup: marks running/spawning agents as stopped
- [x] Biome lint pass: fixed all lint warnings across apps/ and packages/

---

## V1 Tasks (T01-T16) — All Complete

### Cluster A — Agent Lifecycle & Monitoring

#### T01 — Wire Git Worktrees into Agent Spawn
- [x] Built @exegol/core-rust native module (napi-rs) — produces .node + index.js + index.d.ts
- [x] Wired Rust git2 functions into AgentManager (create_worktree, worktree_has_changes, remove_worktree)
- [x] When `useWorktree` is checked: creates isolated git worktree before PTY spawn, sets cwd to worktree path
- [x] On agent exit: checks if worktree has changes — if no changes, auto-removes worktree + branch; if changes, keeps it
- [x] Records worktree in DB (`worktrees` table) and links to agent (`agents.worktree_id`)
- [x] Auto-generates branch name from task description: `exegol/<slugified-description>` (lowercase, hyphens, max 50 chars)
- [x] Added `branchName` optional field to `AgentCreate` schema and type
- [x] SpawnAgentDialog passes branchName to spawn mutation when worktree is enabled

#### T03 — Token Usage Monitor (JSONL Parser)
- [x] Created `main/tokens/log-parser.ts` — parses Claude Code JSONL logs from `~/.claude/projects/`
- [x] Extracts input_tokens, output_tokens, model fields; estimates cost using per-model pricing table
- [x] Added `scan` mutation to tokenUsage tRPC router — imports log entries into `token_usage` DB table
- [x] Added `history` query to tokenUsage tRPC router — returns raw records for last N days
- [x] Rewrote TokensSection: summary cards (cost, input/output tokens, tool calls), model cost breakdown with CSS bars
- [x] Added `useTokenHistory` and `useTokenScan` hooks to use-trpc.ts
- [x] Updated `useTokenUsageSummary` to accept optional `projectId` parameter
- [ ] ~~Parse Codex or Aider JSONL logs~~ (deferred)
- [ ] ~~Token cost in sidebar AgentMiniCard~~ (deferred)

#### T11 — Re-launch Stopped Agents
- [x] Added re-launch button (RotateCw icon) to AgentMiniCard for stopped/completed/failed agents
- [x] On click: spawns new agent with same `{ projectId, cliType, taskDescription }`, creates terminal, focuses it
- [x] Original agent preserved in history
- [x] Button shows spinner while re-launching

#### T14 — Per-Agent Process Metrics
- [x] Added `getAgentProcessMetrics()` in `main/system/resources.ts` — runs `ps -o pid=,pcpu=,rss= -p <pids>`
- [x] Updated `getProjectMetrics()` to accept optional `agentPids` array
- [x] Updated resources tRPC procedure to collect running agent PIDs from AgentManager + DB
- [x] ResourcesSection active agents table now shows CPU% and Memory columns with real data
- [x] Maps agent IDs to process metrics via PID lookup from DB agents

### Cluster B — Terminal & Editor

#### T02 — Diff Viewer
- [x] Created `main/ipc/procedures/diff.ts` — tRPC router with `projectDiff` and `stagedDiff` queries
- [x] Rewrote `DiffSection.tsx` — full diff viewer with unstaged/staged toggle, unified/split view modes, auto-refresh, file stats
- [x] Created `diff/diff-parser.ts` — manual unified diff parser (no external libraries)
- [x] Created `diff/DiffFileView.tsx` — collapsible per-file section with file path, +/- counts
- [x] Created `diff/DiffHunkView.tsx` — hunk header + colored diff lines, both unified and split (side-by-side) views
- [x] Added `useDiff(projectId, mode)` hook
- [ ] ~~Syntax highlighting (Shiki)~~ (superseded by Monaco Editor)

#### T08 — Terminal Split Panes
- [x] Created `TerminalInstance.tsx` — extracted reusable xterm.js component from TerminalPanel
- [x] Refactored `TerminalPanel.tsx` — delegates to TerminalInstance, handles stopped agent detection + scrollback replay
- [x] Created `TerminalSplitView.tsx` — tree-based split layout using `react-resizable-panels`
- [x] Updated `stores/terminals.ts` — PaneNode type, paneLayouts state, split/close/set actions
- [x] Keyboard shortcuts: Cmd+D (split horizontal), Cmd+Shift+D (split vertical)
- [x] Per-pane agent assignment with PaneAgentSelector

#### T16 — Terminal Scrollback Persistence
- [x] Per-agent scrollback buffer (string array, capped at 1MB), flushes to `{userData}/scrollback/{agentId}.log`
- [x] Periodic scrollback flush every 30s (crash safety)
- [x] Created `main/ipc/procedures/scrollback.ts` — tRPC router with `get` and `exists` queries
- [x] Stopped agents fetch scrollback and render read-only xterm
- [x] Path traversal guard in scrollback.ts

### Cluster C — Workspace Tabs

#### T04 — Task Viewer from Markdown
- [x] Created `renderer/lib/markdown-tasks.ts` — parser for `- [ ]`/`- [x]` tasks
- [x] Created `main/ipc/procedures/files.ts` — tRPC procedures: readFile, writeFile, pickFile, listDirectory
- [x] Rewrote `TasksSection.tsx` — file picker, progress bar, interactive checkbox list, auto-detect TODO.md
- [ ] ~~Filter: all / pending / completed~~ (deferred)

#### T09 — Prompts & Templates
- [x] Full CRUD prompt templates per project with categories (task, review, debug, custom)
- [x] One-click copy, pre-fill SpawnAgentDialog, pinned prompts in sidebar
- [x] Migration 011_prompts, PromptsSection, 7 hooks

#### T10 — File Explorer Panel
- [x] Tree view with lazy-loading, .gitignore filtering, directory-first sort
- [x] Click file -> read-only Monaco viewer with syntax highlighting
- [x] Integrated into AgentsSection as toggleable secondary panel

### Cluster D — Infrastructure Services

#### T05 — Internal Scheduler Engine
- [x] SchedulerEngine class with Cron jobs via croner
- [x] 8 scheduler queries, 8 tRPC endpoints
- [x] Event-based completion (replaced 5s polling with onAgentComplete callbacks)
- [x] Concurrent execution guard, 10-minute polling timeout

#### T07 — Port Detection
- [x] detectListeningPorts (lsof + CWD filter), parseConfigPorts, getProjectPorts
- [x] PortBadges in ProjectsSection, click to open in browser
- [ ] ~~Conflict warning for duplicate ports~~ (deferred)

### Cluster E — Settings & Appearance

#### T06 — Open in IDE
- [x] IDE launcher via shell PATH resolution (vscode, cursor, zed, intellij, webstorm, custom)
- [x] OpenInIdeButton component in ProjectsSection

#### T12 — Theme System (Light/Dark/System)
- [x] Light/dark CSS variables, system auto-detection, immediate apply
- [x] Terminal colors match theme, smooth CSS transitions (xterm excluded)

#### T13 — Recent Sessions (DB-backed)
- [x] Last 10 completed/stopped agents across all projects
- [x] Click navigates to project

#### T15 — API Key Management
- [x] safeStorage encryption with plaintext fallback
- [x] Provider rows (Anthropic, OpenAI, Google) with inline edit
- [x] Keys injected into agent env on spawn

### Post-V1 Improvements
- [x] Split queries.ts (576 LOC) into 7 domain files under db/queries/
- [x] Migration 012: added `source` column to token_usage table
- [x] Scheduler: event-based onAgentComplete callbacks
- [x] IDE opener: validates binary on PATH
- [x] Diff viewer: binary file detection
- [x] TasksSection: auto-probes TODO.md, todo.md, TASKS.md, tasks.md, plan.md, PLAN.md
- [x] Theme transitions: smooth CSS transitions for bg/color/border
- [x] Biome lint: 115 files, 0 errors, 0 warnings

### Workspace Rebuild & Monaco Editor Integration
- [x] WorkspaceTabBar (create, rename, close tab)
- [x] WorkspacePane (terminal, browser, files, empty/agent-selector)
- [x] WorkspaceLayout (recursive split with react-resizable-panels)
- [x] Zustand workspace store persisted to localStorage
- [x] Monaco Editor replacing Shiki (50+ languages, VS Code-quality)
- [x] AgentLauncher (sidebar quick-launch bar with colored CLI icons)
- [x] Browser pane (Electron webview with URL bar)
- [x] CronBuilder visual component
- [x] Auto-save settings, dead code cleanup

---

## V2 Tasks (T17-T34) — All Complete

### Agent 1 — Real-Time & Dashboards

#### T17 — Push-First Agent Status Updates
- [x] `broadcastAgentStatus()` emits IPC events on status change (spawning->running->completed/stopped)
- [x] Renderer subscribes via `window.api.onAgentStatus(callback)` in Zustand agents store
- [x] Polling removed for agent list refresh, push-first with 30s fallback
- [x] tRPC query on mount for initial hydration
- [x] Doc: `docs/applied/T17_push_status.md`

#### T18 — Resources Dashboard (Real Metrics UI)
- [x] System overview: CPU %, RAM used/total, disk used/total with progress bars
- [x] Per-agent process table: PID, CPU %, RAM MB, uptime
- [x] Mini sparkline chart (last 30 data points, inline SVG)
- [x] Threshold indicators: yellow >70%, red >90%
- [x] Push metrics via IPC event (T17 pattern)
- [x] Doc: `docs/applied/T18_resources_dashboard.md`

#### T19 — Token Usage Dashboard (Cost Tracking)
- [x] Per-model token breakdown (input/output/cache) with costs
- [x] Per-agent cost table (total tokens, total cost, avg cost per session)
- [x] 30-day trend chart (inline SVG sparklines)
- [x] MODEL_PRICING extensible (hardcoded but structured)
- [x] Session cost available via useAgentCosts hook
- [x] Doc: `docs/applied/T19_token_dashboard.md`

#### T20 — Activity Feed
- [x] Activities table in DB with event types (agent_spawned, agent_stopped, agent_completed, agent_failed, scheduler_fired, port_detected)
- [x] Scrollable timeline with type icons and relative timestamps
- [x] Filter by event type
- [x] ActivitySection workspace tab
- [x] Doc: `docs/applied/T20_activity_feed.md`

### Agent 2 — Terminal, Session & Search

#### T21 — Terminal SerializeAddon for Buffer Persistence
- [x] @xterm/addon-serialize integrated in TerminalInstance
- [x] Serialized state (not raw text) stored in scrollback files
- [x] Stopped agents restore full terminal state (colors, formatting intact)
- [x] Fallback: raw text if serialize fails (try/catch with null)
- [x] Doc: `docs/applied/T21_serialize_addon.md`

#### T22 — Tab Recovery Tokens
- [x] Each pane type implements recovery token pattern
- [x] RecoveryToken: { type, agentId?, filePath?, url?, metadata? }
- [x] Workspace store reconstructs panes from tokens on startup
- [x] Graceful handling: deleted agents, missing files -> empty pane with message
- [x] Doc: `docs/applied/T22_recovery_tokens.md`

#### T23 — Project-Wide Full-Text Search
- [x] FTS5 virtual table indexing (agent scrollback, prompts, task descriptions, scheduler results)
- [x] Index updated on agent completion (scrollback -> FTS5)
- [x] Search UI: query input, results with highlighted matches, click to navigate
- [x] SearchSection workspace tab
- [x] Snippet extraction with context + BM25 scoring
- [x] Doc: `docs/applied/T23_fulltext_search.md`

### Agent 3 — Orchestration & Communication

#### T24 — Agent Provider Registry
- [x] AgentProvider type with capabilities (supportsWorktree, supportsResume, supportsRPC, supportsVision)
- [x] 11+ built-in providers (claude, codex, aider, gemini, goose, opencode, amp, kiro, kilocode, crush, shell)
- [x] Custom providers via Settings
- [x] AgentLauncher reads from registry (not hardcoded list)
- [x] Doc: `docs/applied/T24_provider_registry.md`

#### T25 — Inter-Agent Messaging (SQLite Mail)
- [x] Messages table (id, from_agent_id, to_agent_id, type, content, created_at, read_at)
- [x] Types: text, handoff, status, request, result
- [x] tRPC: sendMessage, getMessages, markRead
- [x] MessagesSection workspace tab
- [x] Doc: `docs/applied/T25_agent_messaging.md`

#### T26 — Dependency-Aware Scheduler
- [x] Scheduled tasks have optional `depends_on` (array of task IDs)
- [x] Dispatcher resolves dependency graph before spawning
- [x] Blocked tasks show "waiting on: [task names]"
- [x] Concurrency limit: max N agents per project (configurable)
- [x] Cycle detection prevents circular dependencies
- [x] Doc: `docs/applied/T26_dependency_scheduler.md`

#### T27 — Context Handoff on Token Limit
- [x] Token limit warning detection in stdout (pattern matching)
- [x] Handoff generation: current task, progress, files modified, next steps
- [x] Stored in handoffs table (linked to agent)
- [x] "Continue with new agent" button spawns successor with handoff injected
- [x] Doc: `docs/applied/T27_context_handoff.md`

#### T34 — Agent Task Queue (Parallel Execution)
- [x] task_queue table (id, project_id, prompt, cli_type, priority, status, depends_on, timestamps)
- [x] Queue statuses: queued, running, blocked, completed, failed, cancelled
- [x] Execution modes: sequential/parallel (max N), priority-based
- [x] Concurrency limit per project: default 3, configurable
- [x] QueueSection UI with status, reorder, cancel
- [x] "Add to queue" option alongside "Spawn now"
- [x] Doc: `docs/applied/T34_task_queue.md`

### Agent 4 — Git Ops (Rust) & Quality

#### T28 — Rust-Native Diff Generation
- [x] Rust fn: `get_diff(repo_path, staged)` via git2 in `packages/core-rust/src/git/diff.rs`
- [x] FileDiff: { path, hunks, binary, status: Added/Modified/Deleted }
- [x] tRPC procedures call Rust (napi-rs) instead of `git diff`
- [x] Performance: <50ms for typical project diffs
- [x] Doc: `docs/applied/T28_rust_diff.md`

#### T29 — Oplog / Undo Timeline
- [x] Oplog table (id, agent_id, operation, ref_before, ref_after, timestamp)
- [x] Operations: commit, branch_create, worktree_create, file_write
- [x] Undo: reverts to ref_before (safe commit, never force-push)
- [x] OplogSection: timeline of agent git actions with undo buttons
- [x] Doc: `docs/applied/T29_oplog_undo.md`

#### T33 — Quality Scoring for Agent Output
- [x] Tier 1 (free, auto): files_changed, compiles, tests_pass, task_completed (parse stdout)
- [x] Tier 2 (structured): exit_reason, turns_used, tokens_spent, files_modified_count
- [x] Store score per agent session in agent_scores table
- [x] ScoringSection: success rate by CLI type, by project, over time
- [x] Non-fatal: scoring never blocks agent completion (try/catch)
- [x] Tier 3 LLM-as-judge: future (gated behind API key)
- [x] Doc: `docs/applied/T33_quality_scoring.md`

### Agent 5 — MCP, Skills & Memory

#### T30 — MCP Host (Tool Proxy)
- [x] MCP client: connect to stdio and HTTP MCP servers
- [x] Tool discovery: list available tools from connected servers
- [x] Tool proxy: route agent MCP requests to correct server
- [x] Settings: add/remove MCP servers (name, transport, command/url)
- [x] Dynamic instructions: build context string from available tools
- [x] Doc: `docs/applied/T30_mcp_host.md`

#### T31 — Skills System (Markdown-Based, with Sharing & Personas)
- [x] Format: markdown with YAML frontmatter (name, description, category, role, requires)
- [x] Discovery: project-local (`.exegol/skills/`) + global (`~/.exegol/skills/`)
- [x] Global skills shared across all projects; project skills override by name
- [x] Enable/disable toggle per project (stored in DB)
- [x] Requirement checking: skip skills with unmet CLIs or env vars
- [x] Role/persona support with named cognitive frameworks
- [x] 5 default global skills: architect, qa, debugger, reviewer, documenter
- [x] SkillsSection workspace tab with browser, preview, toggle, role badge
- [x] Doc: `docs/applied/T31_skills_system.md`

#### T32 — Agent Memory System
- [x] Memory table (id, project_id, category, content, source_agent_id, created_at, relevance_score)
- [x] Categories: preference, pattern, error, solution, dependency, convention
- [x] Extraction: on agent completion, parse scrollback for extractable knowledge
- [x] Injection: on spawn, retrieve top-N relevant memories -> inject into prompt
- [x] MemorySection: memory browser in workspace (list, search, delete)
- [x] Doc: `docs/applied/T32_agent_memory.md`

---

## Summary

| Phase | Tasks | Count | Status |
|-------|-------|-------|--------|
| V1 (T01-T16) | Agent Lifecycle, Terminal, Workspace, Infra, Settings | 16 | All Complete |
| V2 (T17-T34) | Dashboards, Search, Orchestration, Git/Quality, MCP/Skills/Memory | 18 | All Complete |
| **Total** | | **34** | **34/34 Complete** |
