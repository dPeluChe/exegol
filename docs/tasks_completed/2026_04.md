# Tasks Completed — April 2026

## 2026-04-23: Activity visual, access mode badge, pipeline mode propagation (v0.4.3)

### T70 — Activity Classification (DONE)
- [x] `classifyActivity(status, step)` in `packages/shared/src/types/agent.ts`
- [x] `AgentActivityLevel` type (`busy | idle | neutral`) + `AGENT_ACTIVITY_LEVELS` const
- [x] Store auto-computes `activityLevel` on every push event and `syncFromDb`
- [x] `StatusDot` component accepts optional `activityLevel` prop (stops pulse when idle)
- [x] **Tab chrome dot**: `WorkspaceTabBar` shows a 1.5px colored dot (green pulsing = busy, amber = idle) when tab's primary pane has a running agent
- [x] 9 unit tests for `classifyActivity` covering all 9 agent statuses + null/undefined step variants

### T58 — Runtime Permission Modes (PARTIAL — spawn + pipeline done)
- [x] `AgentAccessMode` type (`read | write | plan`) + migration `029_agents_access_mode`
- [x] `buildShellCommand` injects access mode instruction as prompt prefix
- [x] `EXEGOL_ACCESS_MODE` env var set at spawn time
- [x] `SpawnAgentModal` mode selector (Full Access / Plan Only / Read Only buttons)
- [x] **Access mode badge** in live terminal toolbar: "read-only" (blue) or "plan-only" (purple) pill shown when `accessMode !== "write"`
- [x] **Pipeline step `accessMode`**: `PipelineStepDef` gains optional `accessMode` field; executor propagates it to `createAgent` + `manager.spawn`; `PipelineTemplateEditor` shows per-step Mode selector
- [ ] Runtime mode switching (change mode while agent is running) — deferred
- [ ] Scheduler task `accessMode` propagation — deferred

---

## 2026-04-03: Quality Foundation & Monolith Decomposition

### T74 — Test Coverage Foundation
- [x] Vitest configured with v8 coverage provider
- [x] 8 test suites, 138 tests passing (108 initial + 30 title-status tests)
- [x] Covered modules: scoring, handoff, spawn-env, status-parser, extractor, memory store, pipeline context, ring-buffer, title-status
- [x] Test files co-located with source (not separate __tests__ dir)

### T75 — Monolith File Decomposition
- [x] WorkspacePane.tsx: 800→308 LOC (→ BrowserPaneContent, EmptyPaneContent)
- [x] ProjectsSection.tsx: 634→56 LOC (→ AgentMiniCard, BranchGroup, ProjectItem)
- [x] manager.ts: 629→381 LOC (→ agent-worktree-ops, agent-session-callbacks, reattach-sidecar-agents)
- [x] pty-host.ts: 660→400 LOC (→ pty-session-types, pty-shell-ready, pty-scrollback, pty-legacy-session)
- [x] executor.ts: 581→310 LOC (→ pipeline-helpers, pipeline-step-handler, pipeline-worktree)
- [x] Unified reattach/spawn callbacks via createSpawnCallbacks (eliminated duplication)
- [x] Added AgentContext type (replaced unsafe `as Agent` cast)
- [x] Removed dead pipeline-state-machine.ts

### T61 — Real Worktree Isolation per Agent (PR #13)
- [x] Wired Rust git2 into agent spawn flow
- [x] One worktree per agent with deterministic branch naming
- [x] Worktree metadata persisted, auto-cleanup on agent exit
- [x] Surface worktree path and branch in UI

### T62 — Review Readiness + Risk Summary (PR #11)
- [x] Review summary with risk signals before diff
- [x] Files touched, commands run, tests detected, failure signals

### T63 — Desktop Performance Stabilization (PR #12)
- [x] Push-first status broadcasting (reduced polling to 30s fallback)
- [x] Cached heavy resource calls
- [x] Centralized refetch constants

## 2026-04-04: Quick Wins

### T56 — Agent Status via Terminal Title (tests added)
- [x] Added 30 unit tests for title-status.ts (extractTitleFromData, detectStatusFromTitle, titleStatusToAgentStatus, createTitleStatusTracker)
- [x] Tests cover: OSC extraction (BEL/ST terminators), Gemini/Claude/Braille symbol detection, keyword matching, debounce behavior, status transitions

### T64 — Command Palette
- [x] CommandPalette.tsx component with fuzzy search + keyboard navigation
- [x] Cmd+K / Cmd+Shift+P toggles palette (Cmd+Shift+P reassigned from "Go to Projects")
- [x] Commands: navigation (projects, settings, sidebar), workspace (tabs, panes, splits), agents (new, stop, focus running agents)
- [x] Async project search from DB when query ≥ 2 chars
- [x] Grouped results by category with shortcut hints
- [x] Arrow keys + Enter + Esc keyboard-only navigation

## 2026-04-05: Quick Wins & Polish

### T76 — Replace curl with SDK in Tier 3 Scoring
- [x] Replaced `execFile("curl")` with native `fetch()` — no new dependencies
- [x] Added `AbortSignal.timeout(30_000)` for request timeout
- [x] Added `logger.warn` on API error instead of silent skip

### T72 — Dark-Black (OLED) Theme
- [x] CSS variables: `[data-theme="dark-black"]` with pure #000000 backgrounds
- [x] Terminal theme: `DARK_BLACK_TERMINAL_THEME` with black background
- [x] Settings UI: "OLED Black" option with Monitor icon
- [x] Schema/types updated: `"dark-black"` added to theme enum

### T79 — MCP Auto-Reconnection
- [x] `StdioTransport.onDisconnect` callback fires on process exit
- [x] `McpHost.scheduleReconnect()` with exponential backoff (2s→32s, 5 attempts max)
- [x] Overlapping timer guard, proper cleanup on intentional disconnect
- [x] `disconnectAll()` clears all timers

### T66 — Session Isolation & Deterministic Resume
- [x] Migration 026: `session_id TEXT` column on agents table
- [x] Session ID persisted on spawn (session_id = agent.id)
- [x] `resumeSession` field in AgentCreate schema + types
- [x] Manager appends provider's `resumeFlag` (e.g. `claude --continue`) when resuming
- [x] TerminalPanel "Resume" button passes `resumeSession: true`
- [x] `useResumableCliTypes()` hook reads from provider registry (not hardcoded)

### T67 — Agent Hook Event System
- [x] `AgentEventType` structured union: task_complete, task_failed, permission_needed, token_limit, tool_use, prompt_submit, session_start, stop
- [x] `logAgentEvent()` / `getAgentEvents()` — DB persistence in `agent_events` table
- [x] Migration 027: `agent_events` table with index
- [x] `emitAgentEvent()` — programmatic event emission
- [x] `cleanupOldEvents()` — TTL cleanup (30 days), runs on startup
- [x] 4 unit tests for event emission lifecycle

### FileExplorer — Inline Input
- [x] Replaced `window.prompt()` with inline `<input>` in file tree
- [x] Auto-focus, Enter/Escape/blur handling with `handledRef` dedup
- [x] Parent dir auto-expands when creating inside collapsed folder

## 2026-04-10: Pre-launch Polish Wave (v0.3.0)

Branch: `feature/default-view-ralph-cli` — 21 commits.
Full release notes in `docs/CHANGELOG.md#030--2026-04-10`.

### T83 — Smart Git Button in GitPane
- [x] `diff.gitState` tRPC: branch, hasUpstream, ahead/behind, dirty counts, conflicts, PR state (via gh CLI), ghInstalled
- [x] `diff.createPullRequest` / `diff.mergePullRequest` wrapping gh pr create/merge
- [x] `diff.suggestCommitMessage` — Claude Haiku generates conventional-commit message from current diff
- [x] gh CLI detection cached on first use
- [x] `SmartGitAction` component with 11 context-aware states (conflicts, commit, push, push-new-branch, create-PR, merge-PR, view-PR, pr-merged, pr-closed, install-gh, up-to-date)
- [x] Sparkles button on commit input for AI message generation
- [x] Replaced old Commit/Push button pair in GitPane

### T84 — Picture-in-Picture Pane Float (terminal + browser)
- [x] `apps/desktop/src/main/windows/floating.ts` — per-paneId BrowserWindow map, frameless + alwaysOnTop
- [x] JSON-RPC protocol: `floating:open` / `close` / `self-close` / `self-devtools` / `closed`
- [x] Renderer routes on `?floatingPane=...` query string → lazy `<FloatingPaneRoot/>`
- [x] Terminal float shares PTY via snapshot replay (no duplicate xterm instances)
- [x] Browser float: full back/forward/reload + DevTools toggle (targets webview webContents, drops alwaysOnTop while open)
- [x] `FloatingPlaceholder` in source pane with "Return to pane" button
- [x] `use-floating-pane-sync` hook auto-unmarks on traffic-light close
- [x] PictureInPicture2 button in PaneToolbar + "Float to Window" in PaneContextMenu

### T85 — Layout Presets (built-in + custom saved)
- [x] 6 built-in presets: Single, Split Horizontal, Split Vertical, Three Columns, Bottom Terminal (70/30), 2×2 Grid
- [x] `lib/layout-presets.ts` with pure-function helpers (`computePresetTransformation`, `computeCustomPresetTransformation`, `templateFromLayout`)
- [x] `LayoutPresets` dropdown in tab bar with SVG glyph previews
- [x] Preset `slotTypes` — Bottom Terminal auto-spawns shell agent
- [x] Custom saved layouts: capture per-slot type + url + filePath, persisted globally in workspace store
- [x] "My Layouts" section + "Save current layout…" inline input
- [x] Extras preservation: overflow panes nested into last slot

### T86 — First Paint Optimization
- [x] Deferred ensureDefaultSkills / ShellWrappers / AgentWrappers to background IIFE
- [x] Deferred stale agents + memories cleanup to background
- [x] `startMark`/`endMark` instrumentation with single-log guard
- [x] Measured: dbInit 2-9ms, criticalPath 4-12ms, windowCreated 80-102ms, firstPaint 277-391ms (dev mode, M1 Pro)
- [x] `docs/BENCHMARKS.md` documents the full breakdown

### T87 — Renderer Bundle Audit
- [x] rollup-plugin-visualizer + `ANALYZE=1 bun run build:analyze`
- [x] Lazy-loaded: TerminalInstance (595 KB), workspace sections (31-53 KB each), SettingsPanel (51 KB), ProjectList, CommandPalette, FloatingPaneRoot
- [x] Zod + schemas auto-split into shared chunk (113 KB)
- [x] index.js: **1,987 KB → 1,026 KB (−48%)**
- [x] Remaining weight documented (react-dom, tailwind-merge, Radix, etc.)

### Recovery fix — dead sidecar sessions stuck as "running"
- [x] New `session.listInfo` RPC returns `{ id, alive, exitCode, signal }`
- [x] Sidecar tracks exitCode + signal in onExit handler
- [x] `SIDECAR_VERSION` bumped 1.0.0 → 1.1.0 — existing discovery auto-shuts down + respawns old sidecars
- [x] Main process: only ALIVE session ids feed into `reattachSidecarAgents`, dead ones fall through to crash sweep
- [x] Fallback to `session.list` if listInfo fails (older sidecar) without tearing down the connection
- [x] `ReattachResult { reattached, aliveIds, deadIds, failedIds }` — caller uses `aliveIds` only
- [x] Clean up half-initialized runtime state (output processors, scrollback buffers) on dead/failed reattach
- [x] Comprehensive `[Startup]` / `[Reattach]` / `[Recovery]` / `[PaneRecovery]` / `[AgentStore]` logs

### Agents IPC idempotency + projects.get null
- [x] `agents.stop` / `getStatus` / `updateStatus` / `get` return null instead of throwing NOT_FOUND
- [x] `projects.get` returns null for stale persisted activeProjectId
- [x] `useAutoSelectProject` hook auto-picks first project on cold start
- [x] Removed redundant stale-project guard from ProjectContext
- [x] Eliminated "Agent X not found" console spam during close

### Cmd+W closes pane, not window (macOS app menu)
- [x] Platform-aware modifier in `use-hotkeys` (metaKey on macOS, ctrlKey elsewhere) — Ctrl+W no longer hijacks native text-editing
- [x] New `apps/desktop/src/main/windows/app-menu.ts` custom macOS menu
- [x] File → Close Pane bound to Cmd+W (replaces default Close Window)
- [x] Cmd+Q still quits, Cmd+T opens new tab
- [x] IPC route: menu → `menu:new-tab` / `menu:close-pane` → useHotkeys handlers

### Bundled Nerd Fonts (~6.8 MB)
- [x] MesloLGS NF Regular (MIT, from powerlevel10k-media)
- [x] FiraCode Nerd Font Mono Regular (OFL, from nerd-fonts v3.2.1)
- [x] JetBrainsMono Nerd Font Mono Regular with ligatures (OFL)
- [x] `styles/fonts.css` with @font-face rules, imported from globals.css
- [x] electron-builder auto-packages via `out/**/*` glob — Vite hashes the asset URLs
- [x] `.gitattributes` marks *.ttf/*.otf/*.woff binary
- [x] Lazy-loaded by the browser — only the active font is actually read from disk

### Terminal font settings UX rewrite
- [x] Top card: Font Size input + Font Family as clickable badges + inline preview
- [x] Badge click promotes family to primary; × removes from chain
- [x] `monospace` displays as "System default"
- [x] Grouped list: Bundled (with "Included" badge) vs External (with Install link)
- [x] Per-card preview rendered in each font's own face (exercises Nerd Font glyphs + ligatures)
- [x] Case-insensitive, quote-agnostic chain comparison (`normalizeFamily` helper)
- [x] Multi-word families auto-quoted on select
- [x] Bottom preview shows language logos (node/python/rust/java/swift with correct nf-dev-* codepoints)
- [x] 12 fonts in recommended list (3 bundled + 9 external references)

### Browser pane navigation
- [x] Back / Forward / Reload buttons in URL bar
- [x] `canGoBack` / `canGoForward` tracked via webview events
- [x] Empty state overlay with Retry button on did-fail-load
- [x] Empty state dismissed on did-start-loading

### Workspace as default view + stale project recovery
- [x] `useAutoSelectProject` hook — cold start picks first project; runtime deletions auto-jump to another
- [x] `handleSelectFont` / `handleDeselectFont` now case-insensitive

### Misc fixes
- [x] Terminal "Starting crush..." overlay stuck on reattach → snapshot probe on mount
- [x] Layout presets dropdown clipped by tab row `overflow-x-auto` → moved outside scrollable container
- [x] `terminal:get-snapshot` IPC handler lazy require crash → use top-level import
- [x] firstPaint re-logged on window recreation (149902ms bogus) → single-log guard
- [x] `applyLayoutPreset` returns `{ terminalsToSpawn }` for shell auto-spawn
- [x] Custom layouts preserve slot types (type + url + filePath) on save
- [x] Nerd Font dev icon codepoints in preview (node/python/swift)

### Docs
- [x] `docs/CHANGELOG.md` created — v0.3.0 release notes
- [x] `docs/BENCHMARKS.md` — measured numbers + recovery telemetry examples
- [x] `docs/TASK_TODO.md` — T83-T87 marked completed, post-launch backlog (T88-T94) inspired by Paseo + Superconductor
- [x] `CLAUDE.md` — new sections for Layouts, PiP, Smart Git Button, Key patterns updated, monorepo structure expanded
- [x] apps/desktop version 0.2.0 → 0.3.0

## 2026-04-12: Infrastructure Wave (v0.4.0)

Branch: `feature/v0.4.0-infra-wave` — 9 commits.
Full release notes in `docs/CHANGELOG.md#040--2026-04-12`.

### T57 — Review Inbox / Attention Center
- [x] AttentionItem model: 3 levels (critical/action_needed/info), persisted via zustand persist
- [x] Agent Monitor in sidebar: running agents grouped by project with animated pet spinners (10 unique animations per agent)
- [x] Attention inbox below running agents: cards with level dot, provider icon, reason, time ago
- [x] Click card → navigate to agent's pane (switch project + tab + focus)
- [x] Hover actions: mark read (Eye), pin/unpin, dismiss (X), clear read
- [x] Badge count in sidebar "Agents" header + StatusBar "N needs attention" amber indicator
- [x] Unread state derived from attentionItems (removed redundant unreadAgents map)
- [x] unreadAttentionCount cached as O(1) field (not filter-on-every-render)
- [x] Sorted attention: useMemo in component (not in store getter)
- [x] 10s tick interval for elapsed time updates

### T77 — DB Row Validation with Zod Schemas
- [x] 14 Zod row schemas in `packages/shared/src/schemas/db-rows.ts`: project, agent, worktree, prompt, scheduledTask, scheduledResult, tokenUsage, activity, memory, score, oplog, queueTask, message, skillState
- [x] Helper types: sqlBool (preprocess 0/1→boolean), optStr/optNum (.catch(null))
- [x] parseRow<S>() generic helper: safeParse + warn-on-failure (graceful degradation)
- [x] All 7 mapper functions in helpers.ts rewritten to use parseRow
- [x] Exported from packages/shared/src/schemas/index.ts

### T89 — Exegol CLI Scaffold
- [x] New package: packages/cli/ with esbuild bundler
- [x] `exegol status` — lists active agents with color-coded status dots
- [x] `exegol projects` — lists all projects with agent counts
- [x] `exegol search <query>` — functional semantic search over indexed project (cosine similarity)
- [x] DB resolution for macOS/Windows/Linux (follows Electron app data convention)
- [x] `exegol help/version` — standard output

### T96 — Bang Commands in Command Palette
- [x] Detect `!` prefix in Cmd+K input → bang command mode
- [x] Terminal icon (accent) + hint bar when in bang mode
- [x] Spawns one-shot shell agent, creates new terminal tab, injects command
- [x] Shell-ready detection: listens for first PTY data event before injecting (replaced hardcoded 500ms setTimeout)
- [x] 2s fallback timeout with boolean guard to prevent double injection

### T99 — Agent Access Modes (read/write)
- [x] AGENT_ACCESS_MODES const + AgentAccessMode type
- [x] accessMode field on Agent type + AgentCreate schema
- [x] agentAccessModeSchema Zod validator
- [x] DB migration 029: `access_mode TEXT NOT NULL DEFAULT 'write'`
- [x] createAgent persists accessMode, mapAgentRow includes it

### T100 — Project Indexing with Ollama Embeddings
- [x] indexer/ollama-client.ts: checkOllamaStatus (availability + model check), generateEmbedding, generateEmbeddingsBatch
- [x] indexer/chunker.ts: line-based 500/50 overlap chunking, SHA-256 hashing, language detection, exclude patterns
- [x] indexer/project-indexer.ts: async FS walk, incremental indexing (hash-based skip), progress callback, cleanup deleted files
- [x] DB migration 030: file_index + file_chunks tables with indexes
- [x] tRPC: indexer.ollamaStatus, indexer.projectStats, indexer.startIndexing
- [x] Settings UI: OllamaStatusSection with URL/model inputs + Verify Connection button (green/amber/red indicators, install hints)
- [x] Ollama URL + model persisted in Settings type (not local state)

### T68 — Semantic Search (via Vector Index)
- [x] indexer/search.ts: brute-force cosine similarity over embedding BLOBs
- [x] cosineSimilarity extracted to packages/shared/src/lib/cosine.ts (shared between main + CLI)
- [x] tRPC: indexer.search (projectId + query + topK → SearchResult[])
- [x] CLI: exegol search <query> — resolves project from CWD, embeds query via Ollama, computes similarity, prints top-5 with colorized output

### Code Quality (simplify review)
- [x] Removed redundant unreadAgents map — derived from attentionItems
- [x] getSortedAttention moved from store getter to useMemo in component
- [x] getAttentionCount replaced with cached unreadAttentionCount O(1) field
- [x] cosineSimilarity deduplicated (shared lib)
- [x] Triple markRead call reduced to single delegation
- [x] project-indexer switched from sync FS to async fs/promises
- [x] Bang command setTimeout(500ms) → shell-ready signal + 2s fallback
- [x] Ollama settings persisted in Settings type (not local useState)

## 2026-04-12: Type Safety Wave

Branch: `claude/pipeline-state-type-safety-8oj2N`

### T78 — Explicit Pipeline State Machine
- [x] `PIPELINE_TRANSITIONS` map: pending→running, running→paused/completed/failed/cancelled, paused→running/cancelled, terminal states reject all
- [x] `canTransition(from, to)` — pure boolean check
- [x] `assertTransition(from, to)` — logs warning on invalid, returns false (no throw)
- [x] Guards in executor.ts: `advanceStep`, `completeRun`, `pauseRun`, `cancelRun` all check before DB update
- [x] Guards placed before side effects (agent stop) so invalid transitions don't trigger unnecessary work
- [x] 32 tests: all valid transitions, all terminal state rejections, assertTransition behavior

### T82 — Shared Package Schema Enrichment
- [x] `packages/shared/src/schemas/mcp.ts`: mcpTransportSchema, mcpServerConfigSchema, mcpToolCallResultSchema
- [x] `packages/shared/src/schemas/scheduler.ts`: scheduledTaskStatusSchema, scheduledResultStatusSchema, scheduledTaskCreateSchema, scheduledTaskUpdateSchema
- [x] `packages/shared/src/schemas/token-usage.ts`: tokenUsageSummarySchema, tokenUsageQuerySchema
- [x] `pipelineRunTransitionSchema` added to existing pipeline schema (complements T78)
- [x] All new schemas exported from `packages/shared/src/schemas/index.ts`
- [x] MCP procedure: replaced local mcpServerConfigSchema with shared import
- [x] Scheduler procedure: replaced inline create/update schemas with shared imports

## 2026-04-12: Parallel Agent Wave (3 agents, 6 tasks)

PRs #24, #25, #26 — executed by 3 parallel Claude Code agents from Exegol.

### T78 — Explicit Pipeline State Machine (PR #24)
- [x] `pipeline/state-machine.ts`: PIPELINE_TRANSITIONS map with terminal states
- [x] `canTransition(from, to)` + `assertTransition(from, to)` (warns, doesn't throw)
- [x] Guards inserted in `pauseRun`/`cancelRun` before side effects
- [x] 32 new tests covering valid, invalid, and terminal-state transitions

### T82 — Shared Package Schema Enrichment (PR #24)
- [x] `packages/shared/src/schemas/mcp.ts`: mcpServerConfigSchema, mcpToolCallResultSchema
- [x] `packages/shared/src/schemas/scheduler.ts`: schedulerCreateSchema, schedulerUpdateSchema
- [x] `packages/shared/src/schemas/token-usage.ts`: tokenUsageSummarySchema, tokenUsageQuerySchema
- [x] `pipelineRunTransitionSchema` complementing T78
- [x] MCP + Scheduler tRPC procedures now use shared schemas (removed inline duplicates)

### T80 — Structured Error Classification (PR #25)
- [x] `lib/errors.ts`: ExegolError → TransientError / PermanentError / TimeoutError hierarchy
- [x] `isTransient()` / `isPermanent()` type guards
- [x] `withRetry<T>(fn, opts)`: exponential backoff (1s base, max 3), only retries TransientError
- [x] 19 tests for hierarchy, guards, and retry behavior
- [x] Scoring API errors classified as TransientError (example adoption)

### T91 — Lifecycle Scripts per Repo (PR #25)
- [x] `lifecycle/loader.ts`: loadLifecycleConfig reads `.exegol/lifecycle.yaml` or `.yml`
- [x] `runSetupIfNeeded()` wired into manager.ts before agent spawn (fire-and-forget)
- [x] `beforeAgent` command prepended to fullCommand in manager.ts
- [x] `teardown` awaited in cleanupWorktree (now async) before removeManagedWorktree
- [x] 12 parser tests + 5 loader tests

### T95 — Focus-Aware Panel Targeting (PR #26)
- [x] `getFocusedOrFirstPaneId(tab)` in workspace.ts: returns focusedPaneId if in active tab, falls back to findFirstPaneId
- [x] WorkspaceTabBar handleNewTerminal uses focus-aware targeting
- [x] SpawnAgentModal + QuickLaunchBar use focus-aware targeting
- [x] Null-safe fallback: when focusedPaneId is null, behaves as before

### T69 — Diff Review with Line Comments (PR #26)
- [x] Migration 031: diff_comments table (project_id, agent_id, file_path, line_number, hunk_index, content, resolved, created_at)
- [x] `queries/diff-comments.ts`: list, create, delete, toggleResolve
- [x] `procedures/diff-comments.ts`: tRPC router with Zod validation (content max 5000 chars)
- [x] `DiffLineComment.tsx`: CommentInput + CommentCard inline UI components
- [x] `use-trpc-diff-comments.ts`: hooks with optimistic updates
- [x] Per-file lazy fetch (only when expanded)

## 2026-04-15: Agent Monitor & Session Resume

Branch: `feature/agent-monitor-view` — 6 commits merged to main.

### Agent Dashboard View
- [x] `AgentDashboard.tsx`: full monitor grid (345 LOC) with live agent cards
- [x] Card data: uptime, token usage (k tokens + cost), status dot, provider icon
- [x] Read-only badge for access mode, session/agent ID (8 chars, tooltip full ID)
- [x] Navigation session loss fix in WorkspaceTabs/WorkspaceView

### T101 — Agent Session Resume
- [x] DB migration 032: `claude_session_id TEXT` column on agents
- [x] DB migration 033: `resume_command TEXT` column on agents
- [x] Rust `parse_session_id()`: Claude startup banner parsing (session ID)
- [x] Rust `parse_resume_command_pattern()`: generic shutdown output parsing (all CLIs)
- [x] `AgentOutputStream` constructor: `new(cliType, resumeCommandPattern)` — config-driven, zero CLI branching
- [x] JS fallback: `parseResumeCommandFromPattern()` mirrors Rust logic
- [x] `AgentProviderCapabilities.resumeCommandPattern` type + Zod schema
- [x] Registry: patterns for claude-code, codex, gemini, opencode, factory-droid
- [x] `agent-session-callbacks.ts`: captures resumeCommand/sessionId from output, writes to DB, broadcasts via IPC
- [x] Manager 3-tier resume priority: resume_command → claude_session_id → static resumeFlag
- [x] `AgentStatusEvent.claudeSessionId` push event field
- [x] Renderer store: `claudeSessionId` + `accessMode` in AgentState
- [x] TerminalPanel: passes `resumeFromAgentId` on resume click
- [x] 22 Rust tests passing (12 original + 10 new: session ID + resume command)
- [x] All 10 `addAgent` call sites updated with new fields

## 2026-04-23: QA Automation, DI Context, Chat View, Virtual Scrolling (v0.4.2)

Branch: `feature/wave-t90-t59-t81` — merged to main.

### T102 — Design Mode + QA Test Automation
- [x] `design-capture.ts`: `DESIGN_MODE_INJECTION_SCRIPT` injects selection overlay, captures selector + styles + HTML; `formatElementForAgent()` formats for agent stdin
- [x] `qa-recorder.ts`: `QA_MODE_INJECTION_SCRIPT` records clicks/inputs/keypresses/navigation; `formatRecordingForAgent()`, `exportToPlaywright()`
- [x] `qa-replay.ts`: `replayQaTest()` executor — injects helpers, drives steps sequentially, captures screenshots with 3s timeout, collects console errors; `stopOnFail` option
- [x] DB: `qa_tests` + `qa_test_runs` tables (migrations 034–035); tRPC router `qaTests` with list/get/save/delete/getLatestRun/saveRun procedures
- [x] `BrowserPaneContent.tsx`: Design Mode toggle (crosshair) + QA Mode toggle (bug); stop-on-fail checkbox; replay progress (step counter, spinner); save-test flow with name input; result display (pass/fail badges per step)
- [x] `QaTestsSection.tsx`: Project sub-tab listing saved tests; `TestCard` with expand/run/delete (confirm); `RunDetails` showing step results and errors
- [x] Event bus: `exegol:qa-run-test` dispatches to focused browser pane; `exegol:switch-section` navigates to agents tab on run
- [x] Post-review fixes: focused-pane guard (multi-pane), cache invalidation after save, screenshot timeout, run error banner, stale savedTestId on new recording, double-save prevention
- [x] Named constants extracted: `DESIGN_POLL_INTERVAL_MS`, `DESIGN_AUTO_STOP_MS`, `QA_NAV_DELAY_MS`

### T90 — Terminal ↔ Chat Dual View (live agents)
- [x] `ChatView.tsx` + `terminal-to-chat.ts` already implemented for stopped agents
- [x] Added live-agent toggle: `liveSnapshot` state + `handleToggleLiveView` captures `terminalRef.current?.serialize()` on switch
- [x] Toggle toolbar shown only when `hasData === true` (avoids showing before first output)
- [x] `TerminalViewToggle` shared component used by both stopped-agent banner and live toolbar
- [x] 34 unit tests in `terminal-to-chat.test.ts`

### T59 — Virtual Scrolling for Memory List
- [x] `@tanstack/react-virtual@3.13.24` installed
- [x] `MemorySection` replaced `<ScrollArea> + .map()` with `useVirtualizer` — `measureElement` for variable heights, `estimateSize: 72`, `overscan: 5`
- [x] Padding moved to outer scroll container (border-box fix); items use `width: 100%`
- [x] Item cache via local variable (eliminated double array access)

### T81 — DI for tRPC Singletons
- [x] `context.ts` now injects all 5 singletons: `agentManager`, `providerRegistry`, `pipelineExecutor`, `schedulerEngine`, `mcpHost`
- [x] `procedures/agents.ts`: `getAgentManager()` → `ctx.agentManager`, `getProviderRegistry()` → `ctx.providerRegistry` (all 8 call sites)
- [x] `procedures/scheduler.ts`: `getSchedulerEngine()` → `ctx.schedulerEngine` (6 call sites)
- [x] `procedures/pipeline.ts`: `getPipelineExecutor()` → `ctx.pipelineExecutor` (4 call sites)
- [x] `procedures/resources.ts`: `getAgentManager()` → `ctx.agentManager`
- [x] `procedures/mcp.ts`: `getMcpHost()` → `ctx.mcpHost` (4 call sites); procedures that had no `ctx` param updated

---

## 2026-04-24: QA Hardening, FloatingBrowser Extraction, Spawn Perf (v0.4.4)

Branch: `feature/qa-review-fixes` — PR #28.

### T102 — QA Replay hardening
- [x] `assert` action type: replays DOM assertion steps; pass/fail reflected in step result without navigation
- [x] Per-step `alertsDetected` / `newConsoleErrors` collected after each step action
- [x] `scrollIntoView({ block: "center" })` called before every click to ensure visibility
- [x] Interactive ancestor bubbling in click detection: walks up to 5 levels to find `<a>/<button>/[role=button]` when click target is an inner element (e.g. icon inside button)
- [x] Scroll steps are no-op guarded: skip the 500ms delay + 3 IPC calls for pure scroll actions
- [x] Post-step: `Promise.allSettled([alertQuery, errorQuery, screenshot])` — three operations run concurrently instead of sequentially

### FloatingBrowser extraction (T84 follow-up)
- [x] `apps/desktop/src/renderer/FloatingBrowser.tsx` extracted from `FloatingPaneRoot.tsx` (517 → 103 lines)
- [x] `IssueBubble` shared sub-component used by both `BrowserPaneContent` and `FloatingBrowser` — duck-typed `AgentRef { id, cliType }` accepts both `Agent` and `AgentState`
- [x] Replay state (`replayStep`, `replayResults`, per-step detail display) shared between floating and embedded views via same component

### Shared constants
- [x] `RUNNING_STATUSES = new Set<AgentStatus>(["running", "waiting_input"])` exported from `packages/shared/src/types/agent.ts`
- [x] `ACTIVE_STATUSES = new Set<AgentStatus>(["running", "spawning", "waiting_input"])` exported alongside
- [x] All scattered `new Set(["running", "waiting_input"])` literals replaced across renderer

### Spawn lifecycle performance
- [x] **API key cache**: `Map<string, string | null>` in `security/keystore.ts` — `safeStorage.decryptString` (crypto-expensive) runs once per session per provider; invalidated on store/delete
- [x] **Lifecycle config cache**: `Map<string, LifecycleConfig | null>` in `lifecycle/loader.ts` — eliminates duplicate `existsSync+readFileSync` on repeated spawns from the same project
- [x] **`activateAgent`** in `db/queries/agents.ts`: single `UPDATE agents SET pid=?, session_id=?, status='running'` replaces 3 separate post-spawn DB round-trips
- [x] **`-l` flag removed** from shell spawn: `_getFullPath()` already captures full PATH at startup via `zsh -ilc 'echo $PATH'`; `PATH` injected explicitly in agent env; login shell loading no longer needed (saves ~100-150ms dotfile overhead per spawn)

### qa-recorder.ts
- [x] `INTERACTIVE_TAGS` and `INTERACTIVE_ROLES` RegExp hoisted to IIFE scope — no re-compilation on every click event

### Memory leak fixes
- [x] `designAutoStopRef` / `startTimerRef` cancel-on-unmount guards in `BrowserPaneContent`
- [x] `WorkspacePane` stale-closure fix: event listener uses ref-stable callback

### Test coverage
- [x] 64 new unit tests for T58/T70/T90/T101/T102 features
- [x] All suites passing; biome + typecheck + build clean
