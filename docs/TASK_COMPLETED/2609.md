# Tasks Completed: September 2026

## 2026-09-26: resume a Claude session by name (branch `feat/resume-named-session`)

- [x] The Launch modal, for Claude Code in the project folder, lists that folder's Claude
  sessions (last 30 days) by their `/rename` name, else the AI title: picking one launches
  `claude --resume <id>` (the id is kept on the row, so a crash resumes the same one).
  `--continue` only reached the latest, useless in a folder with several
- [x] The Claude history reader takes the name from the transcript tail (`custom-title` is
  re-appended through the file; the head only has the AI title), `readTail` in `read-head.ts`
- [x] Security: spawned sessions no longer inherit `EXEGOL_*` from Exegol's own environment.
  Exegol started from an agent's terminal (dev) handed that agent's MCP token and id to every
  shell it opened; found by a test that failed only when run inside an Exegol session

## 2026-09-26: sidebar badges survive a narrow sidebar (branch `fix/sidebar-narrow-badges`)

- [x] Narrowing the sidebar clipped the running/paused badges, section counts and the Projects
  "+" before the names: section titles, project and group names and the Dashboard label truncate
  (`min-w-0`), the badges and counts are `shrink-0`

## 2026-09-25: add a browser or terminal beside a pane (branch `feat/pane-add-sibling`)

- [x] A terminal pane's hover bar has "Add a browser beside" (globe) and a browser pane "Add a
  terminal beside": pick To the right or Below. The browser opens on the project's preferred or
  running port (`lib/project-browser-url.ts`, shared with the launcher's Browser button); the
  terminal is a shell in the project. `splitPane` takes an optional pane `id` so the caller can
  spawn into the new pane
- [x] The pane drag handle is in the accent color (it stays first in the bar)

## 2026-09-25: app-wide tooltips (branch `feat/app-tooltips`)

- [x] Every `title` in the UI (about 230 files) now shows the app's own tooltip instead of the
  native macOS one (slow, unstyled). `TitleTooltips`, mounted once in `main.tsx` for the main,
  settings and floating windows, lifts the title on hover (so the native tooltip never appears),
  shows a themed bubble after 400ms (at once when moving between icons), below the element or
  above near the bottom edge, and puts the title back on leave. New UI just uses `title=`
- [x] Same surface as the Radix `TooltipContent`: `tooltipSurfaceClass` exported from `@exegol/ui`

## 2026-09-25: project edit dialog (branch `fix/project-edit-dialog`)

- [x] Name, icon and color in one dialog (right-click → "Edit name, icon and color..."); inline
  Rename stays. The dialog is portaled, so it was not what shifted the sidebar (that was the
  inline rename input, #156)
- [x] The dialog mounts only while open (every project row kept one, with its query hooks)
- [x] The project context menu stays inside the window when opened near the bottom or right edge

## 2026-09-25: sidebar shifted sideways after renaming (branch `fix/sidebar-horizontal-scroll`)

- [x] Renaming a project or group left the whole sidebar scrolled sideways until a reload: the
  rename input (`flex-1` without `min-w-0`) kept its intrinsic ~20ch width, overflowed the row,
  and focusing it scrolled the sidebar horizontally. The inputs shrink to the row now, and the
  sidebar root is `overflow-x: clip` (a hidden box can still be scrolled by focus; clip cannot)

## 2026-09-25: compact bug-report logs, quiet update network error (branch `fix/diagnostics-compact-update-error`)

- [x] Bug-report logs are pasted into agents, so they cost tokens: `compactLog` prints the date
  once per day and `HH:MM:SS` after it, and folds a line that repeats one of the last 6 lines
  (agents interleave) into `(xN, last HH:MM:SS)`. On a real 300-line log: 34 KB → 8.9 KB
- [x] At the source: `tool_use` hook events and the per-tool-call "running" signal are no longer
  logged (turn start/end and attention still are); they were most of the log
- [x] "Update error: net::ERR_FAILED" showed in the title bar: Chromium's generic network failure
  joins the silenced network errors (logged, retried by the periodic check)

## 2026-09-25: suspended sessions shown on the project row (branch `feat/project-paused-badge`)

- [x] A project with suspended sessions shows a pause badge (with a count when more than one)
  next to its running count, and the running count no longer includes them; the collapsed rail
  shows a small pause mark on the project's icon

## 2026-09-25: 0.5.3 performance pass (branch `perf/0.5.3-review`)

- [x] Startup no longer loads Monaco: Vite's preload helper had landed in the `monaco` chunk, so
  `index.js` imported and modulepreloaded all ~8 MB of it (plus its CSS) at launch just to get the
  helper. The helper now has its own `runtime` chunk; Monaco loads when a file is opened
- [x] `@exegol/shared` is `sideEffects: false`: the barrel pulled every zod schema into the
  renderer's startup chunk. `index.js` 861 KB → 743 KB
- [x] Busy-agent spinners are CSS (a strip of frames stepping sideways) instead of a `setInterval`
  re-rendering React every 60-200ms per busy agent; the sidebar and the Dashboard share one
  `AgentSpinner` with the same animation per agent
- [x] Port detection shares one `lsof` scan for 3s: each sidebar project row polls its ports every
  10s and ran the same two scans once per row
- [x] `knowledge.get` (a polled query) ran two `execSync` git calls on the main process; the
  staleness check is async now. The per-chunk PTY callback no longer builds a Set per chunk
- Measured, fine as is: main `windowCreated` 106ms, `firstPaint` 441ms (before this pass)

## 2026-09-25: repo link in the terminal toolbar (branch `feat/terminal-repo-link`)

- [x] The terminal toolbar shows the project's origin as a chip (`github`, `gitlab`...): click opens
  it in a browser pane beside the terminal (the webview session keeps the login), the arrow opens it
  in the system browser. `diff.remoteWebUrl` turns SSH/HTTPS remotes into the web URL and drops
  any credentials embedded in the remote (`lib/remote-web-url.ts`, tested)

## 2026-09-25: files search and editing (branch `feat/files-search-edit`)

- [x] Search box over the Files explorer: file names (fuzzy) or text in files, across the root and
  every run-target subrepo (a workspace root's `.gitignore` ignores its repos, so one search from
  the root found nothing). `fsSearch.projectSearch`, results replace the tree while typing, a text
  hit opens the file at that line. Closes T193.22 (the unused renderer hook)
- [x] Security: `fsSearch.fuzzyFind` / `grep` took any `root` from the renderer (grep could read
  any folder); now a root must be inside a registered project
- [x] The viewer edits text files: "modified" marker, Save / Cmd+S. Before writing it re-reads
  the file and asks if it changed on disk since the edit began (an agent writing the same file).
  Switching files or closing with unsaved changes asks first

## 2026-09-25: browser port status and stop (branch `fix/browser-port-status`)

- [x] Reported: a port chip stayed green after its server died (the page showed
  ERR_CONNECTION_REFUSED), and a server whose terminal was closed could not be stopped from there.
  A main-frame load failure marks that port red and re-detects ports at once; the poll went from
  30s to 10s. A detected (green) chip shows a stop button on hover: confirm, then
  `resources.killDevServer` (same guards as the Monitor panel: listening under this user, not
  Exegol, not an agent's own PTY)

## 2026-09-25: project icon and color (branch `feat/project-appearance`)

- [x] `w3_013`: `projects.color/icon/icon_image`; `Project.color/icon/iconImage`
- [x] `detectProjectIcons`: favicon/icon/logo/apple-touch/src-tauri/public/app candidates in the
  root and every run-target folder (a workspace keeps each app's icon in its repo), images up to
  512KB as data URLs. Found on the real projects: tasky-client's Tauri icons, expostand's app
  icon/favicon, reyesbets' app icon, openrest's prototype SVG (0-7ms each)
- [x] `projects.setAppearance` (image must be an icon file inside the project), `iconImage`,
  `detectIcons`; `ProjectAppearanceDialog` (found icons + the group color/icon picker, reset)
- [x] `ProjectAvatar` in the sidebar row, the icon rail and the title bar; the project color
  feeds the project chips (Needs attention, Dashboard), over the group color

## 2026-09-25: sidebar layout and icon rail (branch `feat/sidebar-layout-rail`)

- [x] Reported with screenshots: resizing hid Projects and its "+" behind a long Agents list (the
  sidebar was one scroll). Each `SidebarSection` now scrolls on its own with a fixed header:
  Agents capped at 45%, Projects fills the rest, Recent/Activity capped when open
- [x] `SidebarRail`: collapsing (header button, the existing shortcut and palette action) shows a
  44px rail instead of hiding everything: expand, Dashboard with the unread attention count, each
  project (initials, live agents badge, amber dot when one waits), Add project, Settings
- [x] 0.5.3 queue recorded on the board (search, project icon/color, editing with save)

## 2026-09-25: launcher folder actions (branch `feat/launcher-folder-actions`)

- [x] Reported: choosing Files in the launcher ignored the selected folder chip. Terminal, Files
  and Git moved into the Run-in row and act on the selected folder (Files roots the tree there, Git
  opens that repo, only offered when the folder is its own repo or the root); Browser stays under
  the agents. The row always shows, so the three buttons exist in projects with nothing to run



## 2026-09-25: files viewer by type, menu, JSON tree (branch `fix/files-binary-and-menu`)

Reported on 0.5.2: no Open in Finder / default app; PDFs and images opened as text in Monaco;
the open file could not be closed; a JSON tree view wanted, like the markdown preview.

- [x] `files.readFile` returns a `kind`: image/pdf (base64, up to 25MB), binary (NUL in the first
  8KB), too-large (>5MB text), text (content/language, unchanged for Tasks). Closes T193.19
- [x] `FilePreview`: images inline (checkerboard), PDFs in a frame from a blob URL, binaries and
  large files offer the default app; header actions on the LEFT (the pane's hover buttons sit
  top-right and covered Close), Esc closes when focus is inside, Open / Finder buttons, text is
  labelled read-only (the viewer never edited, so there is no unsaved state)
- [x] Context menu: Open with default app, Reveal in Finder, Copy path, Copy relative path,
  Rename (dialog; `files.rename` refuses roots/.git and existing targets)
- [x] `files.create` used to overwrite an existing file with an empty one (`flag: wx` now)
- [x] `CodeViewer`: Code/Tree toggle for JSON and JSONL (`JsonTree`, tree first), same bar as
  the markdown preview; epoch-ms numbers show their date
- [x] Security review: \`openExternal\` would RUN a cloned repo's scripts/apps (no quarantine on
  git clones); directories, executables (exec bit) and runnable types (.app .command .sh .pkg ...)
  are revealed in Finder instead. \`rename\` refuses any path with a \`.git\` segment (a rename into
  .git/hooks runs on the next commit) and checks the target itself. Preview cap 15MB and no cache
  (\`gcTime: 0\`); CSP \`frame-src\` gains \`blob:\` (\`*\` does not cover it, the PDF frame was
  blocked); the PDF URL is made and revoked in one effect (StrictMode). JSON tree renders 200
  children per step and opens as Code past 1MB
- [ ] Next: file/text search (backend exists in core-rust + search router), editing with save and
  a modified marker

## 2026-09-25: mute and suspend sessions (branch `feat/mute-suspend`)

Asked for: a finished project kept showing in Needs attention; a way to keep a session without it
asking for attention, or to set it aside and pick it up later.

- [x] `w3_012`: `agents.muted`, `agents.suspended_at`; `Agent.muted/suspendedAt`, store
  `muted/suspended` (from the row on sync)
- [x] Mute (`agents.setMuted`): alive, no Needs attention entry (store refuses the item, the
  current one is dismissed), no attention or finished/failed notifications (`isAgentQuiet` in main)
- [x] Suspend (`agents.suspend`): marks the row, then the normal Stop; the store flag goes first so
  the stop's event adds no attention item; the pane says "Suspended: Resume to continue" and
  Resume reopens the conversation (resume by session id)
- [x] Controls: Mute/Suspend next to Watch in the terminal toolbar (live agents), in the sidebar
  row's context menu; moon/pause marker in the sidebar and Dashboard, "Suspended" label there
- [x] Review (done by hand; the review agent stalled): renderer toasts were the one ungated path
  (driven by status events) and now skip quiet sessions; the Dock badge and main notifications go
  through the gated emit; the attention hotkey and Watch cards read attention items, which quiet
  sessions no longer get. Decision: a muted session that crashes stays quiet too (mute means mute)

## 2026-09-25: Needs attention shows the project (branch `fix/attention-project`)

- [x] Reported on 0.5.2: attention cards showed only the session name. `AttentionCard` now shows
  the project as a `ProjectChip` (same chip as the Dashboard), from `projects.list`

## 2026-09-25: split sizes persist (branch `fix/layout-sizes-persist`)

- [x] Reported on 0.5.2: a resized pane went back to the middle after switching projects. A drag
  only lived inside react-resizable-panels; the store kept the initial sizes and a remount read
  them. `onLayout` now saves the split's sizes (`setSplitSizes`, by path from the tab root,
  debounced 200ms, no-op when unchanged), and a split applies store sizes that differ from its
  current layout, which also makes Equalize splits move the dividers (it only changed the store).
  Nested PanelGroups had one shared id; now per path. Store test added

## 2026-09-25: readable agent steps (branch `fix/readable-step`)

- [x] Reported on 0.5.2: a Devin row in the sidebar showed `1 shell · ↓ select ⡿⣿ 6"` (its status
  bar scraped as the current step). `readableStep` runs on every scraped step, Rust or JS path:
  a leading spinner/icon is stripped, a line with glyphs further in (box drawing, blocks,
  Braille, private-use icons) is dropped and the previous step stays. Tests
- [x] Simplify pass: the filter moved into \`createOutputProcessor\` (both Rust and JS results, and
  reattach's processor); a chrome line now drops its STATUS too (a bar ending in \`?\` read as
  waiting_input and flushed the queue). Stored steps are filtered when read (row mapper,
  reattach broadcast). Rejecting chrome inside the Rust generic parser was weighed and left out:
  two implementations plus a native rebuild for the same result

## 2026-09-25: T197 run in any project folder (branch `feat/run-targets`)

Asked for on the launcher: a terminal already `cd`'d into a subfolder, `npx convex dev` where Convex
is wired, and a way to choose among many commands (Makefiles) by folder.

- [x] `detectRunTargets`: root plus subfolders that are a git repo or have something to run (depth 2
  inside apps/packages/services/libs/projects; no symlinks, no node_modules/dist/...); reuses
  `detectProjectScripts` per folder, whose cache now evicts expired entries. Checked on the real
  workspaces (4 repos in workspace-tasky, 2 Convex apps in weddingplanner), 1-3ms
- [x] Convex: `convex` dependency + `convex/` folder → `npx convex dev`, listed first
- [x] `RunTargets` replaces the "Dev Scripts" row: folder chips, then `terminal here` and the
  folder's commands (pinned first, 4 visible, `+N`); pins in settings (`project_run_pins`);
  a failed launch shows a toast. `resources.scripts` / `useProjectScripts` removed
- [x] `agents.spawn` refuses a renderer `cwdOverride` outside the project; a launcher folder is
  badged project-root, not "Pipeline WT" (every override used to be treated as a pipeline)
- [x] JSON settings helpers moved to `db/queries/settings.ts` (preferred ports reuse them)

## 2026-09-24: files pane, Finder drops, sidebar files (branch `fix/files-and-drop`)

Reported on 0.5.2: the Files pane showed nothing, dropping a file on a terminal inserted no
path, and a quick way to browse and create files from the sidebar was missing.

- [x] Drag and drop: only Exegol's own drags (internal MIME) were accepted, so a file from Finder
  did nothing. The preload exposes `webUtils.getPathForFile` (Electron 32 removed `File.path`);
  Finder files paste as quoted absolute paths. Test added
- [x] Files pane: listing errors were swallowed (a blank pane); the tree now says why. The
  explorer is keyed by its root (its expanded set was seeded from the first root only), and the
  empty-pane launchers clear a `filePath` left from the pane's previous view (a worktree)
- [x] Sidebar `ProjectFiles`: the explorer in each expanded project (create/rename/delete via its
  menu, drag to terminals); a click opens the file in the active tab's Files pane or a new one
  (`openFileInWorkspace`, `Pane.openFile`)
- [x] Review pass: the likely blank-pane cause was the preview column, which only rendered once
  the file content arrived while the tree had already shrunk to 200px (a failed or slow read left
  an empty area); it now shows loading or the read error. A file drag in the sidebar bubbled to
  the draggable project row and reordered projects on drop (stopped at the tree). Reopening the
  same file after closing its preview did nothing (\`openFileAt\` remount key). Delete asks through
  \`ConfirmDialog\` instead of a blocking \`window.confirm\`

## 2026-09-24: dev servers & ports panel (branch `feat/dev-servers-panel`)

Asked for after the shell-restart bug: a way to see which dev ports are up and to clear stuck ones.

- [x] `system/dev-servers.ts`: user-owned TCP listeners (lsof), full name and uptime (ps), cwd
  matched to a project on a path boundary, and the Exegol agent/shell found up the parent chain
  (agent PTY pids); conflicts when two pids share a port. Checked against this machine
- [x] `resources.killDevServer`: only a pid listening right now under this user; SIGTERM, SIGKILL
  after 3s
- [x] `DevServersCard` in Monitor → Resources: project servers first (open in browser, go to the
  terminal, stop with confirmation), other listeners collapsed; refreshes every 10s
- [x] `resources.portConflicts` / `detectPortConflicts` removed (no UI; conflicts now show on the
  port badge)
- [x] Security + simplify pass: Stop refuses Exegol itself and its parents (the dev server running
  it) and an agent's own PTY (use the agent's Stop); SIGKILL only if the pid's start time is
  unchanged (no reused pid). One `listTcpListeners` scan and one batched cwd lookup
  (`getProcessCwds`, was one lsof per pid) shared with the browser pane's port detection; idle
  shells now count as the originating terminal (`LIVE_STATUSES`). Whole list in ~370ms here

## 2026-09-24: shells survive an app restart (branch `fix/shell-survives-restart`)

Reported on 0.5.2: the one terminal that did not come back after reopening (it was running
`pnpm dev`). Log: `Cleaned 1 shell terminal(s)` → `2 alive` in the sidecar → `Killed orphan sidecar
session I76iediE (no live DB agent)`.

- [x] Startup cleanup ran before the sidecar was queried and deleted every shell row, so the orphan
  sweep killed live shell PTYs. It now deletes only ended shells; running ones go through the
  normal reattach, and dead ones are marked crashed by the sweep
- [x] A pane whose shell is crashed opens a fresh shell in the project folder (same pane) and drops
  the old row; `spawnShellIntoPane` shared with the empty pane launcher

## 2026-09-24: simplify pass over 0.5.2 (branch `chore/simplify-0.5.2`)

Four reviewers over everything since 0.5.1 (21 files). Fixed:
- [x] beforeAgent prefix per shell: fish has no `$?` (parse error, so no agent started) and old fish
  no `{ }`; fish gets `begin; end; or ... $status`. Test runs the sh form for real
- [x] Manual Stop: the mark is cleared if the exit never arrives (a later natural exit read as
  stopped); an agent already dead is recorded `stopped`, not `completed`
- [x] YOLO: read through the query layer (`Agent.yolo`, `setAgentYolo`) and inherited on Re-launch
  too (`resumeFromAgentId` always sent); codex and amp accept the flag after their subcommand
- [x] Pipeline: `attachStepAgent` shared by advanceStep and resumeRun; a step whose agent finished
  while the app was closed is judged instead of re-run (test)
- [x] `showProject` shared by `focusPane` and `jumpToAgent`; activity type `agent_${status}`; the
  sidebar's pane-agent set comes from a primitive selector (a cwd/focus change re-rendered rows)
- [ ] Skipped: idle auto-close still finalizes as failed (pipeline-only); "last session" resume from
  the spawn modal has no source row to inherit YOLO from; re-attaching pipelines at startup instead
  of on Resume

## 2026-09-24: T193.24 theme change without rebuilding terminals (branch `fix/t193-24-theme-no-rebuild`)

- [x] `isLight` left the terminal mount dependencies; a separate effect sets `options.theme`.
  A light/dark toggle no longer tears down every xterm and replays each PTY snapshot, and
  Dark ↔ Dark Black (same `isLight`) now reaches open terminals, which it never did

## 2026-09-24: T193.20 global hotkey after closing the window (branch `fix/t193-20-global-hotkey`)

- [x] `mainWindow` is reset on `closed`: the hotkey called `isMinimized()` on the destroyed
  window and threw. The Dock's `activate` calls `showMainWindow` too (it only recreated the
  window when no window at all was open, so a floating or settings window blocked it)

## 2026-09-24: T193.11 pipeline resume after a restart (branch `fix/t193-11-pipeline-resume`)

- [x] Startup pauses running runs, but the step's agent survives in the sidecar; only its
  completion hook was lost, and Resume spawned the step again (two agents on one worktree).
  `resumeRun` now re-attaches when the current step's agent is alive: status running, completion
  hook re-registered, no spawn. Test simulates the restart
- [ ] Not covered: a step agent that finished while the app was closed still re-runs the step

## 2026-09-24: T184.5 beforeAgent never gates the CLI (branch `fix/t184-5-before-agent`)

- [x] `hook && cli` let a failed hook stop the agent with nothing on screen; for stdin-prompt CLIs
  `exec hook && cli` replaced the shell with the hook, so the CLI never ran even on success.
  Now `{ hook\\n} || printf '[Exegol] beforeAgent failed (exit N)...'; cli` (`exec cli` for stdin
  CLIs), verified in zsh for fail, success and the exec path; the safety guard scans the full string
- [ ] Not done: telling the agent itself (a "setup failed" prompt section) and a longer hook timeout

## 2026-09-24: T193.7 manual Stop recorded as stopped (branch `fix/t193-7-manual-stop`)

- [x] `stop()` marks the agent in `stopRequested` before the kill; `onExit` passes it to
  `finalizeAgentStatus`, which records `stopped` (activity `agent_stopped`, no desktop
  notification). Test: a Stop with exit 143 is stopped; a non-zero exit without Stop is failed

## 2026-09-24: sidebar by tabs (branch `feat/sidebar-by-tabs`)

Reported with a screenshot: one tab (Claude + shell + browser) showed "main" twice, the agents
by CLI name under a branch header, then the same tab again under TABS named `claude-code`.

- [x] `TabsOverview({ projectId })`: every tab of the project (not only the selected one), panes in
  layout order: agent rows (alias first), browser by host, files/git; rows select their own agent
- [x] `ProjectAgentGroups`: worktree branches, then "Not open in a tab"; an agent open in a tab is
  listed only there. Branch + ports on one line; the duplicate `main` group is gone
- [x] `AgentMiniCard` goes through `jumpToAgent` (its `navigateToAgent` overwrote the active tab's
  first pane) and shows the alias; `focusPane` shared by `jumpToAgent` and tab rows, and it
  switches to the Agents section (rows did nothing from the Dashboard)
- [x] Simplify pass: dead `isWorktree` prop dropped, inline IIFE made a component

## 2026-09-24: CLI flags audit, YOLO on resume (branch `fix/cli-flags-resume-yolo`)

Reported: `--dangerously-skip-permissions` not applied (a resumed Claude showed "auto mode on").
Every resume form was built from the bare binary (`claude --continue`, `claude --resume <id>`, the
stored resume line), dropping the provider's args. A per-launch YOLO choice was not stored at all.

- [x] Resume forms keep the configured args, after the resume part (where `codex resume <id>`
  accepts them), deduped (clap CLIs reject a flag given twice); the stored resume line's binary is
  replaced by the configured command
- [x] `w3_011_agent_yolo`: the per-launch choice is saved on the row and reapplied on resume
- [x] Flags verified against `--help` of the installed versions (claude 2.1.282, codex 0.155.1,
  gemini 0.50.0, agy 1.2.10, devin 3000.11.3, opencode 1.18.32, amp, kilocode 7.7.2, crush 0.41.0,
  droid 0.202.0): `YOLO_FLAGS` fixed for codex and added for 7 CLIs (single-token forms such as
  `--permission-mode=dangerous`, validated); devin `-c`/`devin -r <id>`, gemini `--resume latest`,
  kilocode/amp resume, crush resume removed
- [ ] Follow-ups: crush launches with `run` (one-shot, exits); amp takes its prompt on stdin only;
  gemini/kilocode/opencode have prompt flags Exegol does not use; a codex `--full-auto` saved in
  settings is not migrated

## 2026-09-24: bring back sessions killed by a restart (branch `feat/recover-lost-sessions`)

After the reboot, the 4 crashed agents had no session id. Resume then ran the provider's generic
"continue the latest" flag, so every crashed Claude agent of a project reopened the same
conversation.

- [x] `recoverLostSessionId` (called by `spawn` when resuming): if the source row has no id, find
  the agent's session in the CLI's own store (`findLostSession` / `pickLostSession` in
  `history/`): started within 5 min after the agent, else the conversation already open when it
  began (a resume); ids owned by other agents in the project are skipped. Stored as
  `resume_command` (every resumable CLI declares its prefix), copied onto the resumed row, and
  the generic-flag store check is skipped once an id is known (one scan instead of two). Checked against the 3 real crashed Claude
  agents: each matched a distinct session (one 11s after launch, two resumed conversations)
- [x] `useResumeAgent` hook: the pane's Resume logic, shared with the Dashboard; the resumed agent
  takes the old one's pane (found by id when the caller has none), its watch pin and its row
- [x] `ResumeButton` on ended fleet cards and on pinned cards whose session ended; the pane's
  Resume ignores a second click while the first spawn is in flight
- [ ] Devin has no history adapter: it still resumes with `devin -r` (latest in the folder)

## 2026-09-24: updater log without headers (branch `fix/updater-log-headers`)

- [x] A 0.5.0 bug report carried GitHub's `_gh_sess` cookie: the silenced "no release" branch
  logged the whole electron-updater message (response headers included). Both branches log the
  first line only
- [x] `redact()` also strips `set-cookie` / `Cookie` headers, for logs written by older builds;
  tested with the block from that report

## 2026-09-24: resume Claude sessions after a reboot (branch `fix/resume-after-crash`)

Reported from 0.5.0: the Mac shut down with 4 agents running (app got SIGTERM at 14:33, booted
16:45). Recovery marked them crashed as designed, but none could resume: `claude_session_id` and
`resume_command` were empty on all four. The id came from a "Session ID:" banner Claude Code no
longer prints, and the resume command only appears on a clean exit.

- [x] `notify.sh` reads the hook payload Claude Code pipes on stdin (drained whole, parsed from the
  first 4KB) and adds `sessionId` to the event file
- [x] `dispatchAgentFileEvent` stores it as `claude_session_id` (claude-code only, id-shaped only,
  latest wins since /clear starts a new session) before the OSC short-circuit; the existing
  resume path already runs `claude --resume <id>`
- [ ] Not covered: Devin and other CLIs without hooks still resume only from a clean exit's output

## 2026-09-24: no automatic token-limit handoff (branch `fix/no-token-limit-handoff`)

Reported from the installed 0.5.0: a resumed Devin session showed "Token limit approaching" 10s
after spawn; each "Continue with new agent" click spawned a successor that did the same, three
exited 1, and "Needs attention" on them opened nothing.

- [x] Removed the whole handoff chain: substring detection (Rust `check_token_limit` and the JS
  mirror matched any line with "context window" or "token limit", and the injected handoff prompt
  itself ended with "context window was exhausted"), handoff creation, the live banner, the
  stopped-agent "Continue" button, `agents.getHandoff`/`continueWithHandoff`, `agent:handoff-ready`.
  CLIs (Claude Code, Devin, Codex) compact their own context; replacing the session was never
  wanted. The `handoffs` table stays (migrations are append-only). A future context warning has to
  come from a provider signal (hook/OSC), not scraped text
- [x] `jumpToAgent` opens the agent in a new tab when no pane shows it (successors were spawned
  without a pane), instead of doing nothing; it never replaces an existing pane
- [x] Simplify pass: the parallel-run comparator's "Open" goes through `jumpToAgent` too
  (`focusAgentPane` overwrote the active tab's first pane and never switched project); dead
  `token_limit` event type and stale ARCHITECTURE entries removed

## 2026-09-24: browser pane opens the running dev server (branch `fix/browser-dev-port`)

- [x] `ports.ts` read the port from the last `lsof` column, which is `(LISTEN)`, so runtime
  detection returned nothing and the browser pane fell back to :3000 (or a config port).
  `parseLsofListenLine` reads the address column; tested against real rows. Port conflicts had
  the same bug. A cwd match now needs a path boundary (`/repo-2` no longer matches `/repo`)

## 2026-09-24: T196 bug reporting (branch `feat/t196-bug-report`)

Before sharing the app: make failures land somewhere and turn them into an issue in one click.

- [x] Errors reach the log file: main uncaught exceptions and unhandled rejections (were console
  only), renderer and child process crashes (`render-process-gone`, `child-process-gone`),
  renderer `error`/`unhandledrejection` and the ErrorBoundary (were DevTools only, via
  `log:renderer-error`); logged `Error`s keep message and stack (JSON.stringify gave `{}`)
- [x] The PTY sidecar's stderr goes to `~/.exegol/logs/sidecar.log` (was `stdio: "ignore"`)
- [x] Tests write logs to a temp dir: vitest runs were rotating the user's real logs away
- [x] Bug button next to Inspect in the title bar: describe, review what is sent (preview open by
  default, the issue is public), copy diagnostics, open the logs folder, or create a GitHub issue
  (`gh issue create` when signed in, else a prefilled issue page with the full report on the
  clipboard). Diagnostics are collected once per dialog; what was reviewed is what gets sent
- [x] Privacy (reviewed against real log shapes): Exegol's own log lines stop carrying prompts and
  agent output at the source (spawns log `commandShape`, status and "printed an error" lines drop
  the step text); `redact()` covers third-party text: API keys and tokens (Anthropic, OpenAI,
  GitHub, Slack, Google, AWS, JWT, private keys, Bearer, `*_TOKEN/KEY/SECRET=`), URL userinfo and
  query strings, non-local URLs, emails, IPs, and paths made generic (folder names named the user
  and client projects). Bounded quantifiers: two rules were quadratic on long blobs
- [x] Renderer error forwarding drops repeats within 5s and caps at 50 per window; Doctor's
  `which` calls get a timeout
- [x] Watch cards (T194 follow-up): A−/A+ and "fit session to card". A plain mirror shows the
  owner pane's grid shrunk to fit, tiny side by side; a sizing card makes the PTY take its grid at
  the chosen font (`cardFont` in `stores/watch.ts`). The last view shown owns the size: the pane
  takes it back on the workspace refit, the card on mount. Input stays filtered either way
- [x] Reattach rebuilds the terminal model at the PTY's last real size (`w3_010` pty_cols/rows),
  not a fixed 120x30 that reflowed everything the CLI drew
- [x] A clean renderer exit (reload/close) is not logged as a crash
- [x] One repo constant (`lib/repo.ts`) for the update feed, Help > Learn More (pointed at an old
  repo name) and reports

## 2026-09-23: T194 Dashboard watch list with interactive mirrors (branch `feat/t194-overview-watch`)

Pin sessions from any project and work on them in parallel from the Dashboard without switching
folders. Each pinned session is a live, interactive mirror of its terminal.

**Dashboard**
- [x] Its own view (`activeView: "dashboard"`) overlaying the workspace: no project is highlighted,
  panes stay mounted at the same size behind it, picking a project lands on its Agents tab
- [x] With sessions pinned it shows only Watching; "Show all agents" reveals the fleet groups
- [x] Empty state offers "Start agent" per project; header says "All projects"
- [x] One `jumpToAgent` for dashboard, sidebar, hotkeys and toasts; `switchSection` leaves the
  dashboard; sidebar attention cards use the session name and aren't repeated per project

**Watching**
- [x] "Watch" toggle on dashboard cards and the terminal toolbar; global, persisted list that
  follows a session across resume (`replaceAgent`)
- [x] Full-height cards, 1-3 per row; collapsed sessions are vertical strips in their row; drag
  strips or card headers to reorder; up to 6 open; a session needing input opens in place
- [x] Wheel over an unfocused mirror scrolls the dashboard; click the mirror to scroll its history

**Mirror mode** (`TerminalInstance mirror`)
- [x] Types into the session but never resizes the PTY, never answers terminal queries, writes no
  pane state; replies (CPR, DA, DSR, DECRPM, OSC/DCS, focus) are filtered (`mirror-input.ts`)
- [x] Renders at the PTY grid (`terminal:get-size` before the snapshot, then `terminal:resized`)
  and fits its font to both dimensions; the rule always terminates (tested over ~7000 card sizes).
  One fit in flight per terminal; canvas renderer (no WebGL context each)

**Terminal sizing and repaint** (found with dev-only `[TermDbg]` logs, since removed)
- [x] Alt-screen fit CEILED rows and relied on a line-height squeeze that never applied (T193.23):
  each fit overflowed the box by a row, the box grew and the PTY kept gaining rows (234x70 →
  240x78 idle), with Claude's input box below the edge. Always the floored fit; `terminal-fit.ts`
  removed; `min-h-0` on the pane box so its height comes from layout
- [x] Mirror font fit flip-flopped between two sizes forever (glyph rounding), rebuilding xterm's
  atlas every frame (50-110ms rAF violations)
- [x] No PTY resize before layout (was 80x24); resizes coalesced (80ms); main drops same-size
  resizes; a size sent before reattach is kept and applied (`PtyHost.pendingSizes`)
- [x] `terminal:redraw`: main jiggles the PTY without telling mirrors; the mount kick runs only for
  alt-screen sessions and once per session (a project switch used to cost two redraws per pane)
- [x] Visibility per view (`windowId:viewId`): a pane unmounting no longer silenced its mirror;
  a view's first report skips the RIS repaint; WebGL detach debounced

**Other**
- [x] Resume with nothing to continue (`claude --continue` in a fresh folder exits 1) starts a
  clean session: `hasLocalSession` asks the CLI's history adapter first (90-day window)
- [x] `bun run kill:dev` stops a stuck dev Electron without touching the sidecar

## 2026-09-22: v0.5.0 pre-build audit (branch `release/v0.5.0`)

Five read-only audits (packaging with a real packaged app, agent lifecycle, pipelines/git/history,
first run with migrations applied to empty/pre-w3_009/live DBs, tech debt) before cutting the DMG.
Fixed here; open items are T193 on the board.

### Packaged app
- [x] Undo loaded `@exegol/core-rust` with a plain require and had no resourcesPath fallback: dead
  in the packaged app. Now imports `coreRust` from `agents/spawn-env` (`ipc/procedures/oplog.ts`)
- [x] Tray icon path pointed at a folder that doesn't exist and wasn't shipped; now
  `extraResources` → `tray-icon.png` (`system/tray.ts`, `electron-builder.ts`)
- [x] Worktree archive hook was a runtime `require` the bundler left unresolved; static import
- [x] Version 0.5.0 (`apps/desktop/package.json`, CHANGELOG, RELEASE)

### Safety
- [x] Permission guard: a redrawn permission dialog scraped as plain `waiting_input` cleared the
  flag, so a queued `agent_send` could type into the dialog and confirm it with Enter. Now only
  leaving `waiting_input` clears it (`spawn-env.ts#broadcastAgentStatus`)
- [x] Oplog v1 undo force-checked-out `project.path`: refuses when the tree is dirty, when HEAD
  moved past the entry, for `worktree_create`, and for agents that ran in a worktree. Undo and
  snapshot restore errors show a toast
- [x] A worktree shared by two agents is not removed while another live agent uses it
- [x] Commit only removes an `index.lock` older than 60s (a live agent git op holds a fresh one)
- [x] `files.delete` refuses a project root, `~/.exegol` and `.git`
- [x] Removing a project asks for confirmation and stops its live agents first
- [x] Skills migration copied legacy skills only when canonical was empty, then deleted the legacy
  dir anyway; now copies every missing skill (`skills/paths.ts`)
- [x] An unparsable `~/.codex/hooks.json` is left alone instead of overwritten; `~/.codex` not
  created when Codex isn't installed
- [x] The MCP socket is unlinked only by the instance that owns it (dev beside packaged)

### Lifecycle and pipelines
- [x] Reverted the main-side status dedup from the readiness PR: title/session broadcasts moved the
  key without a DB write, so the DB could stay stale. The renderer no-op check stays
- [x] Resume keeps the agent's access mode
- [x] crush/opencode/kiro keep scrollback (collected before the parse skip), so `final_output` and
  scoring have text
- [x] Pausing a pipeline held nothing: the killed step's exit advanced the run. Paused runs ignore it
- [x] Pipelines take a task (textarea); `{{task}}` used to be the template name

### Environment and settings
- [x] The login-shell PATH is adopted into `process.env.PATH`, so main's own `gh`/`git` find
  Homebrew when launched from Finder; a shell timeout falls back to Homebrew dirs; `gh` misses are
  not cached
- [x] Diff cache has a 5s TTL (agent edits never went through an invalidating mutation)
- [x] Settings auto-save sends only the changed fields (the mount-time form reverted MCP verbose
  and notification mutes)

## 2026-09-22: T185 daily-readiness audit, first fixes (branch `chore/daily-readiness-audit`, commit 38681bf)

Audit of dead wiring, settings that never saved, and main-process stalls. The fixes below shipped;
open items stay on the board as T185.1-T185.15. Also closes T182.7.

### Startup and failure paths
- [x] DB init failure shows an error dialog and exits. Before: `await initializeDatabase()` with no
  guard left the app running with no window (`main/index.ts`, was T182.7)
- [x] Doctor no longer runs a login shell at import (0.4-1.3s before the window); PATH is warmed
  async after `createWindow` (`warmShellPath`, `system/doctor.ts`, `bootstrap/window.ts`)
- [x] Migration `w3_009_llm_score_columns` adds `llm_clarity`, `llm_completeness`,
  `llm_correctness`, `llm_score` to `agent_scores`. The tier-3 UPDATE in `scoring.ts` failed every
  time before

### Dead links and shortcuts
- [x] Sidebar Prompts link pointed at a nonexistent section `prompts`; now `prompts-skills`
- [x] Dead sidebar Schedulers section removed (`SchedulersOverview.tsx`); scheduler UI is T185.1
- [x] Prompt "Use" button pre-fills the spawn dialog (was dispatching an event nobody listened to)
- [x] Cmd+D, Cmd+Shift+D, Cmd+. and their command-palette entries work: new store action
  `splitFocusedPane`; stop calls `agents.stop`
- [x] Keyboard shortcuts list matches the handlers: Cmd+K / Cmd+Shift+P palette, Cmd+1-9 workspace
  tabs, Cmd+Shift+] / Cmd+Shift+[, splits added (`KeyboardShortcuts.tsx`)

### Settings that never took effect
- [x] `ollamaUrl` / `ollamaModel` added to `settingsSchema` (zod stripped them, so they were never
  saved). `memory.search`, `search.query` and Doctor read saved values through the new
  `db/queries/settings.ts#getAppSettings`
- [x] Global hotkey setting read at startup and re-registered on change
  (`bootstrap/global-hotkey.ts`); invalid or taken accelerators are logged
- [x] "In-app toasts" toggle suppresses agent status toasts (`use-toast-events.ts`)
- [x] Custom IDE from the pane toolbar and Cmd+click passes `customIdePath` from settings

### Render and push cost
- [x] Status push dedup: a repeated (status, step) from the parser no longer does a DB UPDATE +
  broadcast; renderer `updateAgent` returns the same state when nothing changed. Every pane used
  to re-render several times a second
- [x] Scrollback buffer keeps the LAST 1MB instead of the first. Attention tail, `final_output`
  and scoring were reading stale start-of-session text

### Docs and board
- [x] CLAUDE.md, README.md, GUIDES/RELEASE.md corrected against the code (router, migration,
  table, provider, Rust test counts; MCP tools; tabs; archive-on-close; build and install steps)
- [x] TASK_TODO: finished tasks moved here, duplicate Owl IDs renumbered (T186-T189), overlapping
  items merged into T162 (rooms), T170.3 (outbox), T173 (identity), T175.4 (claims), T191
  (provider descriptor), T192 (evaluator); review 2026-09-05 findings #3-#9 filed as T185.11-15

---

## 2026-09-22: T184.1: Pipeline evidence survives the agent committing, plus CI (PR #115)
`added: 2026-08-22`

- [x] `captureGitDiff` used `git diff HEAD`, empty once the agent commits (and the fix-step
  prompt tells it to commit), so the T88v2 gate answered "No diff to judge" and T130 recorded
  nothing. Each run and step now records a Git tree through a temporary index under
  `refs/exegol/pipelines/<run-id>/` and diffs against it (`pipeline/git-evidence.ts`,
  `executor.ts:108-111`, migration `w3_008_pipeline_evidence_base`). Missing baseline pauses the
  run. 16 MiB cap replaces the 1 MB `maxBuffer`
- [x] CI: `.github/workflows/ci.yml` (lint, typecheck, desktop/shared/Rust tests, build on macOS,
  pinned action SHAs); `bun run lint` calls pinned Biome directly. Details:
  `GUIDES/PIPELINE_EVIDENCE_AND_CI.md`

---

## 2026-09-22: Board sweep, tasks shipped earlier and still listed as pending

Moved from TASK_TODO.md; dates are when the work shipped.

### T155: Terminal & Attention UX Pack `added: 2026-07-09` (shipped 2026-07-10)
- [x] Drag file to terminal as `@path` mention (PR #68)
- [x] Cmd+click file paths + bare URLs (PR #68)
- [x] Attention to exact-pane routing (PR #70)
- [x] Terminal input QoL: Shift+Enter, Cmd+←/→, new-output pulse (PR #68)
- [x] Session browser + resume, cross-provider from our DB (PR #72)
- [x] `exegol .` CLI + `exegol://` deep link (PR #73)
- [x] Notification hygiene: toast hover-pause, per-channel kill switches (PR #73)
- Not done, recorded for later: Finder-external drops (needs `webUtils.getPathForFile`), import of
  Claude sessions born outside Exegol (partly covered by T181 history)

### T156: Global Sessions Dashboard `added: 2026-08-11` (shipped 2026-08-11, PRs #90-#92)
- [x] Dashboard is the top-level home tab (`WorkspaceTabs.tsx`, `sections/AgentDashboard.tsx`),
  cross-project, with a live terminal in peek
- [x] `agents.listActive` over all projects (`ipc/procedures/agents.ts:147`)

### T157: Cross-provider Inter-agent Messaging `added: 2026-08-11` (shipped 2026-08-11/12, PRs #93, #98-#102)
- [x] MCP tools `agents_list`, `agent_send`, `message_status`, `message_cancel`, `messages_check`
  on the T145 server, identity from the caller's token
- [x] Delivery at turn boundaries, reply cycle, shim auto-reconnect, hook-less provider fix
- Residuals live on the board: T170 (durability), T171 (human authorization), T175 (consumed
  provenance)

### T160: Session Alias `added: 2026-08-11` (shipped 2026-08-12, PRs #94, #98)
- [x] Nullable `alias` column (`w3_001_agent_alias`), editable chip in the terminal toolbar
  (`common/SessionAlias.tsx`), used as the messaging address; alias survives resume

### T162 phases 1-2: Directed agent links + roles `added: 2026-08-12` (shipped 2026-08-12, PR #101)
- [x] `agent_link` MCP tool + `agent_links` table (`w3_002`, `w3_003`), fired from the
  `broadcastAgentStatus` choke point; roles notify / reviewer / feedback; cross-project origin in
  headers; Settings > MCP Server panel. Rooms stay on the board
