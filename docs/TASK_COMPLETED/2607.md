diff: empty

## 2026-07-05: WT-A — Signal & Attention (T123, T124, T141, T128)

Branch: `feature_signal` (PR #41, one commit per task).

### T123 — Agent status via hooks + OSC 777
- [x] Rust `OscNotifyScanner` FSM in `processing/osc_notify.rs` + JS mirror in `status-parser.ts`
- [x] Per-agent Claude Code hooks file (`~/.exegol/hooks/<agentId>.json`) injected via `--settings`
- [x] `agent:signal` push events with deterministic source (`hook` vs `parser` fallback)

### T124 — NotificationBus + desktop notifications
- [x] Desktop channel (Electron Notification + dock badge) registered on the T-base bus skeleton
- [x] Attention notifications carry the agent's pending question (scrollback tail)

### T141 — Attention Inbox UI
- [x] TitleBar `AttentionQueue` dropdown on the existing T57 `attentionItems` store + Cmd+J jump

### T128 — Terminal URL detector
- [x] `use-terminal-url-detector` hook + "Open preview" toolbar chip → browser pane split

Review fixes (post-PR, applied on merge): OSC delivery via /dev/tty, per-turn notification
throttling, OSC-stripping in notification bodies, signal-type whitelist at the boundary,
stale URL chip cleared on agent stop.

## 2026-07-05: WT-D — Product Surface & Health (T148, T143, T147, T146)

Branch: `feat/wtd-surface` (PR #44, bundled per branch-owner instruction).

### T148 — First-run Onboarding Wizard
- [x] `renderer/components/onboarding/*` first-run flow: CLI detection via provider
      registry, API key setup (keystore), first project, guided spawn suggestion
- [x] `main/system/doctor.ts` health checks, reusable as Settings → Doctor

### T143 — Resource & Memory Hardening
- [x] Per-agent RSS + ring-buffer + PTY count in Monitor → Resources with thresholds
- [x] xterm/WebGL/Serialize disposal order fixed on pane close; terminals-store
      cleanup wired into workspace.removePane
- [x] Ring-buffer global cap with idle-session LRU eviction to disk + reattach reload guard

### T147 — Cost Dashboard + Budgets
- [x] Cost per agent/run/day from token_usage; editable provider price table
      (per-1M display, per-token storage) via settings.updateModelCatalog
- [x] Budgets with 80%/100% alerts through NotificationBus (data currently
      populated by the manual Scan — acknowledged limitation)

### T146 — Project Groups
- [x] `project_groups` migration (w2d_ set), Sidebar group headers with color/
      icon/collapse, drag & drop, disband-to-root via ON DELETE SET NULL

Review fixes (applied on merge): eviction now actually releases ring-buffer
memory; doctor resolves the login-shell PATH; weekly budget dedup uses a stable
period key; alerts only on escalation; stale drag ref cleared; price editor
seeds on edit-click.
