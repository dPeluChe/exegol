# Tasks Completed — August 2026

## 2026-08-11: Verify round 3 — agent status/communication layer (PR #82)

Branch: `fix/verify-round3`. Live joint verification (Antonio + Fable) with claude-code,
codex and opencode; ~13 fixes to the layer Antonio declared "fundamental y prioritaria
para todo el flujo", closed with a /simplify pass over the branch diff.

### Attention lifecycle (T123/T141)
- `Notification` hook added to the claude wrapper — the `permission_needed` attention leg
  never fired before (`agents/wrappers.ts`)
- `waiting_input` alone = turn boundary, NOT attention: inbox items only on
  `needsAttention=true` or final status; answering auto-resolves `action_needed` items
- Non-claude CLIs (no hook signal): scraped running→waiting transition creates the item,
  transition-gated so idle/reattached agents don't earn amber dots
- Needs Attention section floats above running agents; row ⚠ derives from UNREAD items;
  gray idle dot for plain waiting_input
- Pruning everywhere: removeAgent / syncFromDb (identity-stable) / paneless sweep

### Reattach & orphan cleanup
- Reattach lands as `waiting_input` (idle) — activity self-promotes; fixes codex stale spinner
- MCP server start + per-agent token re-arm on reattach (T145 restart continuity)
- Resume command captured from ring tail on reattach (opencode rescue); `resumePattern`
  wired into the reattached output processor
- `PtyHost.killUnclaimed()` — orphan sweep previously no-op'd for unmapped sessions
- Paneless-agent sweep (`use-paneless-agent-sweep.ts`): stops pre-app-start live agents
  with no pane anywhere; protects headless pipeline/scheduler agents

### MCP (verified end-to-end this session, earlier PRs #79-#81)
- Shim NDJSON framing fix (was LSP Content-Length — clients hung on "connecting")
- `memory_list` tool added; FTS AND→OR retry for multi-term recall

### UX
- Ended sessions start with a clean pane (output behind "Show session output"); slim
  stop-reason card; Chat view ANSI strip + scroll; session browser (T155.5)

### Docs
- `RESEARCH/TRINITY_2026_08.md` (feeds T157/T158) · T160 session alias · T161 resume picker
- `ARCHITECTURE/COUNCIL_BASE.md` + `OWL_FLEET_WATCH.md` merged (Antonio drafts, flagged
  for pre-Wave-3 review — back T156–T160)

---

## 2026-08-11: T149 — Orchestration-core Tests (Wave 2.6 P0, PR #84)

Implemented by a claude worktree agent, supervisor-reviewed (the execution model the wave
plan called for). 42 new tests over the crash/corruption-prone surface; suite 493 → 535.

- `db/migrations.test.ts` (8): full 36-base + migration-sets chain on `:memory:`,
  idempotent re-run with schema fingerprint, data preserved across re-run
- `pipeline/executor.test.ts` (9): real executor + real migrated DB, manager/PTY mocked
- `mcp/exegol-server.test.ts` (13): token mint/revoke/unknown → `-32002`, read-mode
  denied `memory_save` `-32001` (DB-derived), mid-session demotion, fail-closed
- `agents/agent-spawn-flow.test.ts` (12): `buildPtyInvocation` branches, resume paths,
  claude hooks `--settings`, lifecycle beforeAgent, command-guard refusal
- **🐛 REAL BUG found by the new tests and fixed**: `running → running` was missing from
  `PIPELINE_TRANSITIONS` (T78 regression) — `advanceStep`'s `assertTransition` returned
  early on every step advance/loop-back, so **every multi-step pipeline stalled after
  step 0**. One-line fix + locking test.
- Seams: `export` on `exegol-server.ts#handleRequest` (test-only, no socket boot in vitest)

---

## 2026-08-11: T150 — T80 Closure + Rust↔JS Parity Vectors (Wave 2.6 P0, PR #85)

Implemented by a claude worktree agent, supervisor-reviewed.

### withRetry wired (was zero call sites — T80 finally closed)
- New `main/lib/anthropic.ts`: `callAnthropicMessage()` — network/timeout/429/5xx →
  Transient (retried, warn-logged), 400/401/403 → Permanent. Wired into: scoring tier-3
  judge, evaluator judges, evidence AI summary, commit-message suggestion (maxRetries 2),
  and `diff.push` (git stderr network phrases only; non-fast-forward/auth rethrown so the
  set-upstream fallback survives).
- Assessed NOT wired (documented): MCP host reconnect (own capped backoff), agent spawn
  (non-idempotent), gh PR ops (duplicate-PR risk).

### Golden parity vectors (JS fallback ↔ Rust source of truth)
- `packages/core-rust/test-vectors/parser-vectors.json`: 65 cases (strip, OSC-777 incl.
  chunk-split partials + shell-ready exclusion, status scraping) consumed by BOTH cargo
  (serde, 3 tests) and vitest.
- **9 real divergences fixed in the JS mirror**, incl. OSC payloads leaking as literal
  text, per-line vs per-chunk strip ordering, anchored error matchers, tool-detection
  priority order, and `handoff.ts` token-limit phrase list aligned to Rust's 5.
- Gates: vitest 600 green · cargo test 55 · clippy 0 warnings.

---

## 2026-08-11: T151 — Security & Hygiene Polish (Wave 2.6)

Branch: `chore/t151-security-hygiene`. Source: `RESEARCH/CODE_HEALTH_AUDIT_2026_07.md`.

- **Capabilities allowlist tightened**: `preload/capabilities.json` — all 31 `"*"` router
  wildcards replaced with explicit procedure lists (198 procedures), generated from the
  procedure modules and cross-checked against every `trpcInvoke/Mutate/Query` call in the
  renderer (159 used paths, 0 missing, no dynamic template-string calls). The preload gate
  already supported arrays — migration was purely the JSON edit it was designed for.
- **Keystore plaintext fallback surfaced**: `logger.warn` on plaintext store
  (`security/keystore.ts`) + new Doctor check `keystore` (safeStorage availability).
  ApiKeysSettings banner already existed.
- **Doctor: duplicate CLI installs** — CLI detection now uses `which -a`/`where`; >1 path
  → warn listing every copy (source: codex Homebrew-vs-bun self-update loop, 2026-07-09).
- **Doctor: stale worktree sweep** — scans `~/.exegol/worktrees` (1.2 GB observed), warns
  on worktrees with no live agent and >7 days untouched; cleanup via existing
  Project worktrees UI (dirty ones preserved by design).
- **coverage/**: already gitignored and untracked — no action needed.

---

## 2026-08-11: T152 — God-module Split (Wave 2.6 P1, PR #86)

Implemented by a claude worktree agent, supervisor-reviewed. Pure mechanical split, zero
consumer edits (export surface byte-identical, `[Startup]` log strings byte-identical).

- `renderer/stores/workspace.ts` 698 → 77: Zustand slice pattern under `stores/workspace/`
  (types 122 · helpers 99 · tabs-panes 342 · custom-layouts 90 · floating-panes 23 ·
  recovery 46). Persist config unchanged.
- `main/index.ts` 607 → 136: new `main/bootstrap/` (startup-timings 19 · window 90 ·
  deep-link 80 · ipc-handlers 129 · recovery 182 with reattach + orphan + crash sweeps).
- Pre-existing bugs flagged, not fixed: dead `if (mainWindow)` guard after assignment;
  `onWorkspaceRehydrate` mutates rehydrated state in place.
- Pending live smoke: app boot, `exegol .` deep link, layout/floating persist on reload.

---

## 2026-08-13: T176 — A quit that actually quits (PR #105)

Closing needed `kill -9`, and an orphaned sidecar from an earlier run survived alongside
the current one. Three separate causes; only the first was the one being looked for.

- **Electron does not quit on SIGTERM.** No handler existed, so the signal was ignored
  outright. `installSignalHandlers` now asks for a normal quit and force-exits after 8s —
  and the forced path runs teardown first, since `app.exit()` emits neither `before-quit`
  nor `will-quit` and would otherwise leave the database open.
- **Teardown was a straight line of sync calls**, so a step that threw skipped every step
  after it, `closeDatabase()` included. Steps are now isolated, named and timed; the log
  says which one is slow instead of us reading a process tree. `markShutdown` moved to
  LAST — it silences console output, so running it first hid every `[Shutdown]` line.
- **The orphaned sidecar had its own root cause**, found in review: a shutting-down sidecar
  unlinked the socket and pid file unconditionally, and discovery spawned the replacement
  without waiting. The old process, exiting ~500ms later, deleted the NEW sidecar's files —
  it kept serving on an unlinked inode with no pid file, so the next launch found nothing,
  spawned a third, and left the second holding the previous run's PTYs. `cleanup()` now
  removes only files this process owns; discovery waits for the predecessor (escalating to
  SIGKILL) before spawning. `SIDECAR_VERSION` → 1.4.0.

Reviewers killed two things that looked right: a teardown deadline that could never fire
(a timer cannot interrupt the synchronous loop it wraps — the deadline now lives only in
the signal handler, where `app.quit()` returns immediately), and a 500-file cap on the
hook-event sweep that would have traded a slow quit for an unbounded disk leak, since
`cleanupOldEvents` prunes a DB table rather than that directory. The sweep moved to
startup, uncapped, off the quit path entirely.

666 tests pass.
